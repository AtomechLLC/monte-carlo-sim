---
phase: 06-blackjack-core-odds-loop
plan: 08
type: execute
wave: 5
depends_on: [06-07]
files_modified: []
autonomous: false
requirements: [BJ-02, BJ-03, BJ-04, BJ-05, BJ-06, BJ-07]

must_haves:
  truths:
    - "The full suite is green with the 388-test baseline intact and every addition accounted for (D-08)"
    - "Both golden parity detectors passed byte-untouched across the whole phase, proving the Hold'em path's external behavior never changed (D-08)"
    - "Every Phase 4/5 trap is confirmed closed or still respected: WR-02 closed, WR-03 respected, 05-REVIEW WR-03 closed, WR-01 untouched"
    - "A real browser confirms the full BJ-02..BJ-07 loop, including the D-12 deck-toggle findability moment (BJ-07)"
    - "A real browser confirms the mid-round mode switch does not strand the blackjack odds panel or replay its choreography (05-REVIEW CR-01/CR-02/WR-02 non-recurrence)"
    - "Every source item in the multi-source coverage audit is confirmed shipped, with no silent gaps (GOAL/REQ/RESEARCH/CONTEXT)"
  artifacts:
    - path: ".planning/phases/06-blackjack-core-odds-loop/06-08-SUMMARY.md"
      provides: "The phase's regression record, coverage reconciliation and human verdict"
      contains: "Multi-Source Coverage Audit"
  key_links:
    - from: "06-08-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "BJ-02..BJ-07 confirmed-shipped reconciliation"
      pattern: "BJ-0"
---

## Phase Goal

**As a** probability explorer using the simulator, **I want to** play a full Blackjack round on its own table — deal, watch bust / dealer-outcome / EV odds converge live, hit or stand, reveal the hole card, and switch the shoe between 1 and 2 decks — **so that** I can see exactly how each new piece of information and each extra deck reshapes the numbers.

<objective>
Close Phase 6: run the full regression bar, reconcile every source item against what actually shipped, and take human acceptance in a real browser with the D-12 findability moment as the headline check.

Purpose: seven plans built the slice in parallel worktrees. This is the only place the whole thing runs together against the real worker, real Motion and a real browser — the three things every jsdom suite in the phase structurally cannot cover. It is also the D-08 gate: seven plans' worth of diffs must have left the shipped Hold'em path externally identical.
Output: no source changes. A documented regression record, a coverage reconciliation and a human verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-blackjack-core-odds-loop/06-CONTEXT.md
@.planning/phases/06-blackjack-core-odds-loop/06-UI-SPEC.md
@.planning/phases/06-blackjack-core-odds-loop/06-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/06-blackjack-core-odds-loop/06-01-SUMMARY.md
@.planning/phases/06-blackjack-core-odds-loop/06-02-SUMMARY.md
@.planning/phases/06-blackjack-core-odds-loop/06-03-SUMMARY.md
@.planning/phases/06-blackjack-core-odds-loop/06-04-SUMMARY.md
@.planning/phases/06-blackjack-core-odds-loop/06-05-SUMMARY.md
@.planning/phases/06-blackjack-core-odds-loop/06-06-SUMMARY.md
@.planning/phases/06-blackjack-core-odds-loop/06-07-SUMMARY.md

<interfaces>
<!-- No new contracts. This plan reads what shipped and verifies it. -->

Reference figures for the reconciliation:
- Pre-phase suite baseline: **37 test files / 388 tests passing** at commit `7d8fb13`.
- D-12 anchors (06-RESEARCH closed forms): natural frequency `64/1326 = 4.8265%` at 1 deck, `256/5356 = 4.7797%` at 2 decks — the 1-deck arm must be measurably higher.
- Byte-frozen drift detectors that must be green AND unmodified: `src/worker/streamingParity.golden.test.ts`, `src/engine/deckParity.golden.test.ts`, `src/worker/simulationApi.test.ts`.
</interfaces>

**Executor environment notes (apply to every task in this plan):**
- A fresh worktree needs `npm ci` before anything else runs.
- Never modify `.planning/STATE.md` or `.planning/ROADMAP.md`.
- Terminate any dev-server process before finishing — including after the checkpoint resolves.
- Commit the SUMMARY before returning.
- This plan changes NO source files. If a gate fails, the fix belongs in the owning plan, not here: record the failure verbatim and route it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression sweep and D-08 accounting</name>
  <files>(no source changes — this task produces the SUMMARY's regression record)</files>
  <read_first>
    - All seven plan SUMMARYs (the per-plan test-count deltas and enumerated test adjustments being reconciled)
    - .planning/STATE.md Blockers (the Phase 4/5 traps whose closure or continued respect is being confirmed)
    - 06-CONTEXT.md D-08 (the Hold'em-unchanged contract)
  </read_first>
  <action>
    Run the three gates in order and capture the exact figures: `npx vitest run` (test-file count, test count, failures, skipped), `npm run lint`, `npm run build`.

    Reconcile the test count against the 37-file / 388-test baseline. The expected shape is 388 pre-existing tests still passing PLUS every case the seven plans added. If the pre-existing 388 is not intact, do NOT adjust a pre-existing test to make it pass — treat it as a real regression and route the fix to the owning plan.

    Enumerate every adjustment made to any pre-existing test file across the whole phase, pulling from the seven SUMMARYs and confirming each against `git diff` over the phase's commit range. The expected set is exactly three, all mechanical:
    1. `src/state/simulationService.test.ts` — the comlink `wrap` mock's namespaced `{ poker, blackjack }` return shape, plus whatever the lazy `getApi()` construction required of the fake-worker assertions (plan 06-03).
    2. `src/App.modeSwitch.test.tsx` + `src/App.modeIsolation.test.tsx` — the consolidated testid import (plan 06-02); the placeholder-copy and zero-button retargets, the added `./state/blackjackSimulationService` mock factory, and the mirror-image blackjack testid sweep (plan 06-07).
    3. `src/App.modeShell.guard.test.ts` — the D-07 and D-13 retargets (plans 06-02 and 06-07).
    Any adjustment beyond these must be justified in the SUMMARY as mechanical or treated as a regression. A change that relaxes an assertion, deletes a case, or adds `.skip`/`.todo` is NOT mechanical and is not permitted.

    Confirm the lazy-transport gate explicitly: `git grep -n 'new SimWorker()' -- src` must return exactly ONE hit, and it must sit inside a function body in `src/state/workerClient.ts` — never at module scope. A module-scope construction would instantiate a real Worker at import time in jsdom the moment `<App />` reaches the blackjack transport, taking down the five frozen v1 suites. Also confirm `git grep -n 'Comlink.wrap(' -- src` returns no module-scope hit.

    Confirm the D-08 gate explicitly: `git diff --stat <phase-base>..HEAD -- src/worker/streamingParity.golden.test.ts src/engine/deckParity.golden.test.ts src/worker/simulationApi.test.ts` must be EMPTY, and those three suites must be green. Also confirm the five v1 acceptance suites (`App.test.tsx`, `App.acceptance.test.tsx`, `App.phase3.acceptance.test.tsx`, `App.modeErrorBanner.test.tsx`, `App.modeSwitchRace.test.tsx`) are unmodified across the phase.

    Confirm the Phase 4/5 trap ledger:
    - **WR-02 (closed this phase):** both worker validators reject a present-but-invalid `deckCount`.
    - **WR-03 (respected, not closed):** `git grep -n "deckCount" -- src/engine/conditioning.ts src/state/gameStore.ts` shows nothing that passes `deckCount: 2` into the Hold'em trial path; the blackjack path is exempt because it never touches the evaluator.
    - **05-REVIEW WR-03 (closed this phase):** `src/ui/HoldemGame.tsx` exists and `src/App.tsx` holds no game state.
    - **04-REVIEW WR-01 (Phase 8, untouched):** `git diff --stat` shows no change to `src/state/pickerStore.ts`.

    Write the **Multi-Source Coverage Audit** into the SUMMARY, one row per source item, each marked COVERED with the plan that shipped it:
    - **GOAL** — ROADMAP Phase 6 goal and its five success criteria.
    - **REQ** — BJ-02, BJ-03, BJ-04, BJ-05, BJ-06, BJ-07.
    - **RESEARCH** — dual-exclusion-set rule; single-trial-services-all-stats cursor algorithm; fixed 12-card budget + the `createDrawer` defensive check; Option A (no rejection sampling); S17 playout and the soft-total demotion loop; dealer-natural priority in `compareToDealer`; natural-frequency anchors; namespaced Comlink surface; CR-02-safe gate release; Pattern 5 restore signal; Pitfall G StrictMode re-implementation.
    - **CONTEXT** — D-01 through D-14 plus D-03a and D-03b.
    - **UI-SPEC** — A1 through A16, the 29 new testids, the copy contract, the accessibility contract.
    If ANY item is not covered, stop and report `## ⚠ Source Audit: Unplanned Items Found` rather than closing the phase.

    Confirm no dev-server or watch process is running before handing off to the checkpoint.
  </action>
  <acceptance_criteria>
    - Test command: `npx vitest run` exits 0 with 0 failures, 0 skipped, and a test count >= 388 plus the phase's additions.
    - Test command: `npm run lint` exits 0 with no new inline ESLint disable comments introduced anywhere in the phase.
    - Test command: `npm run build` exits 0.
    - Behavior: `git diff --stat <phase-base>..HEAD -- src/worker/streamingParity.golden.test.ts src/engine/deckParity.golden.test.ts src/worker/simulationApi.test.ts` produces no output.
    - Behavior: the five v1 acceptance suites are unmodified across the phase.
    - Behavior: `git diff --stat <phase-base>..HEAD -- src/state/pickerStore.ts` produces no output.
    - Behavior: `git grep -n 'new SimWorker()' -- src` returns exactly one hit, inside a function body in `src/state/workerClient.ts`.
    - Behavior: no commit anywhere in the phase's range leaves `npx vitest run src/App.modeShell.guard.test.ts` red — spot-check the two commits that amend it (plans 06-02 and 06-07), which merge the feature and its guard amendment into one commit each.
    - Source: the SUMMARY contains the Multi-Source Coverage Audit with every GOAL/REQ/RESEARCH/CONTEXT/UI-SPEC item marked COVERED and attributed to a plan.
    - Source: the SUMMARY lists every pre-existing-test adjustment with its mechanical justification.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>All three gates are green, the 388-test baseline is intact, every pre-existing-test adjustment is enumerated and justified, both golden detectors are byte-untouched, the trap ledger is reconciled, and every source item is confirmed shipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser acceptance of the Blackjack vertical slice (BJ-02..BJ-07)</name>
  <files>(no source changes — this is a blocking verification of the running app)</files>
  <read_first>
    - 06-UI-SPEC.md "Interaction States & Testid Contract", "Animation Choreography Contract", "Copywriting Contract" (the contract being accepted)
    - 06-CONTEXT.md D-12 and the `<specifics>` block (the deck toggle must make its effect FINDABLE — that moment is BJ-07's whole point)
    - .planning/phases/05-game-mode-shell-store-separation/05-REVIEW.md CR-01, CR-02, WR-02 (the three switch-back defects whose non-recurrence steps 8 and 9 check)
  </read_first>
  <action>
    Present the verification script below to the developer and BLOCK. Do not proceed, do not mark the phase complete, and do not auto-approve — this checkpoint is never auto-advanceable regardless of workflow settings.

    Start the dev server yourself with `npm run dev` in the background, report the local URL, and stop the server once the verdict is in.

    If the standing autonomy directive is active and no human is available to respond, an agent MAY execute the steps directly in a browser and record the observed result as the verdict, clearly labelled as agent-executed rather than developer-confirmed. The steps below are written to be frame-independent — every check is a comparison of observable end states (text content, enabled/disabled, present/absent) rather than a judgement about an animation mid-flight — so an agent driving the browser can evaluate each one without catching a specific frame. Steps 8 and 9 are the two exceptions that require an action DURING motion; if an agent cannot reliably interleave those, it must record them as NOT VERIFIED rather than assumed-pass, and the phase closes with that gap named.

    If any step fails, record it verbatim in the SUMMARY and route the fix to the owning plan (06-01 engine math, 06-03 worker/transport, 06-04 stores, 06-05 felt/CSS, 06-06 odds cluster, 06-07 wiring) rather than patching around it here.
  </action>
  <what-built>
    A complete Blackjack table: deal a round with the dealer's hole card face-down, watch a streamed Monte Carlo run converge on the bust-if-hit percentage, the seven-bucket dealer final-outcome distribution and per-unit EV for Stand versus Hit; hit or stand with live recomputation and a real dealer playout; reveal the hole card early and watch every number recondition; and toggle the shoe between 1 and 2 decks to see the odds shift. Built on the same worker, animation gate and card components Hold'em uses, with Hold'em's behavior unchanged. Automated coverage: an engine property/statistical suite including the D-12 natural-frequency anchor, a worker API suite, store suites with an animation-gate arm/release invariant, component suites, two mirror-image DOM-absence sweeps, a retargeted source-shape guard, and an end-to-end BJ-02..BJ-07 loop suite — on top of the intact 388-test baseline.
  </what-built>
  <how-to-verify>
    Start the dev server with `npm run dev` and open the printed local URL (typically http://localhost:5173).

    1. **Hold'em is unharmed (D-08).** Before touching Blackjack: click `Deal`, watch the odds converge, click `Advance` through flop/turn/river, `Rewind` back, reveal an opponent seat, open `Set Up Scenario` and pick a card. Everything must behave exactly as it did before this phase. Any difference here is a FAIL, not a cosmetic note.

    2. **Switch to Blackjack, idle state.** Click `Blackjack`. Confirm: an idle block reading `No round dealt yet` with the body `Click Deal to start a round. Switch the shoe between 1 and 2 decks to see the odds shift.`; a felt showing two dashed player placeholders and two dashed dealer placeholders with `You` and `Dealer` badges; a control bar with `Deal`, `Hit`, `Stand` and a `1 deck` / `2 decks` segmented toggle; and an odds panel whose trial counter reads `0` with every stat showing an em dash. Confirm `Hit` and `Stand` are dimmed (never red) and `Deal` is not.

    3. **Deal and watch it converge (BJ-02).** Click `Deal`. Two player cards and one face-up dealer card land; the dealer's second card stays face-down. Once the cards settle, the trial counter starts climbing from zero and the numbers move, then settle. Confirm the seven dealer rows read `17`, `18`, `19`, `20`, `21`, `Natural`, `Bust` in that order and that their percentages sum to roughly 100%.

    4. **Read the decision support (BJ-03, BJ-04).** Confirm `Bust if you hit` shows a percentage; the `If you stand` group shows Win / Push / Loss; and the `Expected value` group shows two tiles, `Stand` and `Hit`, each a signed per-unit number like `−0.18 units`. Confirm the Hit tile carries the visible sub-copy `hit once, then stand` — it must be readable text on screen, not a hover tooltip.

    5. **Reveal the hole card early (BJ-06).** Click the face-down dealer card. It flips face-up in place without the row jumping or resizing, the `Dealer` badge gains a total, and every odds number blanks and then re-converges on different values — the distribution should visibly sharpen now that the dealer's hand is known. Click it again: nothing happens (one-way per round).

    6. **Hit and stand (BJ-05).** Click `Deal` for a fresh round. Click `Hit`: a third card joins your hand, the row re-centres, and the odds blank and re-converge. Click `Stand`: the hole flips, the dealer draws cards one at a time at a pace you can follow, `Hit` and `Stand` both disable immediately, and an outcome banner appears reading one of `You win` / `Dealer wins` / `Push` (or `Blackjack — you win` / `Dealer blackjack` on a natural) with a sentence naming both totals. Confirm the banner is NOT red — a lost round is an outcome, not an error.

    7. **THE DECK-TOGGLE MOMENT (BJ-07 / D-12 — the headline check).** Click `Deal`, wait for the numbers to settle, and note the `Natural` and `Bust` rows of the dealer table. Now click `2 decks`. Confirm, in order: every number blanks to an em dash in the same instant; the dealer table's subtitle changes to `Given the cards you can see · 2-deck shoe`; the trial counter restarts from zero and climbs; the numbers settle on VISIBLY DIFFERENT values from the ones you noted — most legibly the `Natural` row. Click `1 deck` and confirm it goes back. **If the numbers do not visibly move, this step FAILS** — making that shift findable is the entire point of BJ-07.

    8. **Switch mid-deal (05-REVIEW CR-01/CR-02 non-recurrence).** Click `Deal` and, while the cards are still visibly flying, immediately click `Hold'em`. Then click `Blackjack` again. The blackjack odds panel must come back to life and show numbers. If it stays dimmed with em dashes forever, the animation gate stranded and this step FAILS.

    9. **Switch back mid-round (05-REVIEW WR-02 non-recurrence).** With a settled blackjack round on screen, click `Hold'em`, then `Blackjack`. The exact table you left must reappear INSTANTLY — same cards, same reveal state, same numbers — with no deal animation replaying and no re-deal. A replayed fly-in is a FAIL.

    10. **Rapid toggling and console hygiene.** Click back and forth between `Hold'em` and `Blackjack` a dozen times quickly, then deal in each. Both games' odds must still converge normally. Check DevTools for errors or React warnings across every step above — the console must be clean.

    11. **Keyboard and focus.** Tab through the blackjack controls and activate `Deal`, `Hit`, `Stand`, the deck toggle and the hole-card reveal with Enter or Space. Confirm a visible focus ring on each, that neither deck segment is ever greyed out under normal play, and that the app is never keyboard-trapped. NOTE: switching game modes now moves focus to the page body (a known, recorded consequence of the D-07 extraction) — confirm that Tab still reaches the switcher afterwards. That behavior is expected, not a FAIL.

    12. **Reduced motion.** Enable your OS "reduce motion" setting and reload. Deal, hit, reveal and stand. Every card must appear instantly in its final position with no fly-in or flip, and every number must still converge normally — no functionality may be gated behind an animation actually playing.

    Stop the dev server when finished.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 6, or describe exactly which step failed and what you saw.</resume-signal>
  <acceptance_criteria>
    - Behavior: the verifier confirms steps 1-12 pass, or names the exact failing step.
    - Behavior: step 7 (the deck-toggle moment) shows visibly different settled numbers at 1 versus 2 decks — no visible shift is a FAIL under BJ-07/D-12.
    - Behavior: step 8 leaves the blackjack odds panel live — a permanently dimmed panel with em dashes is a FAIL.
    - Behavior: step 9 restores the table with no animation replay — a replayed fly-in is a FAIL.
    - Behavior: step 1 shows Hold'em behaving exactly as before the phase — any difference is a FAIL under D-08.
    - Behavior: the browser console shows no errors or React warnings across all steps.
    - Source: the verdict and any observations are recorded verbatim in `06-08-SUMMARY.md`, labelled developer-confirmed or agent-executed.
    - Behavior: no dev-server process remains running after the checkpoint resolves.
  </acceptance_criteria>
  <verify>
    <human-check>Verifier confirms in a real browser: Hold'em's full loop is unchanged; a blackjack round deals with the hole face-down and its odds converge live; bust/dealer-distribution/EV all render with the "hit once, then stand" basis visible; early reveal reconditions the numbers; hit and stand play out with an outcome banner in non-destructive colour; toggling 1 vs 2 decks visibly moves the settled numbers and retitles the dealer table; a mid-deal mode switch does not strand the odds panel; a switch-back restores the table instantly with no replay; the console stays clean; and reduced motion collapses every choreography without gating any functionality.</human-check>
    <automated>npx vitest run &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>The verifier has approved (or all reported failures have been fixed in their owning plans and re-verified), the verdict and its provenance are recorded in the SUMMARY, and no dev server is left running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main thread -> real Web Worker | Every automated suite in the phase mocks the service. Steps 3, 7, 8 and 10 are the only exercise of the real Comlink boundary, real namespacing and real cancellation. |
| real Motion -> animation gate | jsdom forces reduced motion, so every duration is 0 there. Steps 3, 5, 6, 8, 9 and 12 are the only exercise of real, non-zero choreography against the gate. |
| user input -> both games' state | Steps 8-11 exercise rapid, interleaved and keyboard-driven input the deterministic suites never produce. |
| shipped Hold'em behavior -> the phase's seven diffs | Step 1 is the human-observable half of the D-08 contract that the golden tests cover mechanically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-42 | Denial of Service | animation gate + worker runs against the REAL worker and REAL Motion | mitigate | Steps 8 and 9 exercise mid-flight mode switching and switch-back restoration — the exact CR-01/CR-02/WR-02 shapes — against machinery the mocked jsdom suites structurally cannot reach. A stranded gate or a replayed choreography fails the checkpoint. |
| T-06-43 | Repudiation | BJ-07's claim that deck count changes the odds | mitigate | Step 7 requires a VISIBLE shift in settled values, not merely a re-run. This is the only place the D-12 findability claim is checked as a user experience rather than as a seeded statistic. |
| T-06-44 | Tampering | shipped Hold'em behavior across seven parallel diffs | mitigate | Task 1's byte-untouched golden gate plus step 1's human walk of the full Hold'em loop. Two independent detectors for the same contract. |
| T-06-45 | Spoofing | EV(Hit) mistaken for a basic-strategy calculator's number | mitigate | Step 4 requires the `hit once, then stand` sub-copy to be visible on-screen text, not a tooltip — the D-05 honesty guarantee checked as rendered output. |
| T-06-46 | Denial of Service | functionality gated behind an animation under reduced motion | mitigate | Step 12 walks the whole loop with OS reduced motion enabled and requires every number to converge normally. |
| T-06-47 | Repudiation | agent-executed verification passed off as human sign-off | mitigate | The verdict must be labelled developer-confirmed or agent-executed, and any step an agent cannot reliably interleave (8 and 9) must be recorded NOT VERIFIED rather than assumed-pass. |
| T-06-48 | Tampering | regression-bar erosion across seven plans | mitigate | Task 1 forbids relaxed assertions, deleted cases and `.skip`/`.todo`, enumerates the exactly-three permitted mechanical adjustments, and requires the coverage audit to name a plan for every source item. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase and no new static assets. `npm ci` from the committed lockfile only. An executor reaching for `npm install <package>` must stop and escalate. |
</threat_model>

<verification>
1. `npx vitest run` — 0 failures, 0 skipped, count >= 388 plus the phase's additions.
2. `npm run lint` — exits 0.
3. `npm run build` — exits 0.
4. Both golden detectors and `simulationApi.test.ts` green AND byte-untouched across the phase's commit range.
5. The five v1 acceptance suites unmodified across the phase.
6. Multi-Source Coverage Audit complete with zero MISSING rows.
7. Human (or labelled agent) checkpoint returns "approved".
8. No dev server left running.
</verification>

<success_criteria>
- The full suite is green with the 388-test baseline intact and every pre-existing-test adjustment enumerated as mechanical (D-08).
- Both golden parity detectors passed byte-untouched through seven plans' worth of diffs, and Hold'em's full loop is confirmed unchanged in a browser (D-08).
- The Phase 4/5 trap ledger is reconciled: WR-02 closed, WR-03 respected, 05-REVIEW WR-03 closed, pickerStore untouched.
- A real browser confirms BJ-02 through BJ-07, with the deck-toggle findability moment visibly shifting the settled numbers (BJ-07, D-12).
- The three 05-REVIEW switch-back defects are confirmed non-recurrent for Blackjack (CR-01, CR-02, WR-02).
- Every GOAL / REQ / RESEARCH / CONTEXT / UI-SPEC source item is confirmed shipped and attributed to a plan, with no silent gaps.
</success_criteria>

<output>
Create `.planning/phases/06-blackjack-core-odds-loop/06-08-SUMMARY.md` when done.

Record in the SUMMARY: the final test-file/test counts, the full enumeration of pre-existing-test adjustments with justifications, the golden-gate and trap-ledger confirmations, the complete Multi-Source Coverage Audit table, the checkpoint verdict with its provenance label (developer-confirmed or agent-executed) and any NOT VERIFIED steps, plus any observation the verifier raised that was routed to a follow-up rather than fixed in-phase.
</output>
