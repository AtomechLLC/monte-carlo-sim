---
phase: 07-2-deck-holdem-evaluation
plan: 07
type: execute
wave: 4
depends_on: [07-05, 07-06]
files_modified: []
autonomous: false
requirements: [HE2-01, HE2-02, HE2-03]

must_haves:
  truths:
    - "The full suite is green with the 697-test baseline intact and every addition accounted for (D-11)"
    - "Both goldens and simulationApi.test.ts passed byte-untouched across the whole phase, proving the 1-deck Hold'em path's external behavior never changed (D-11)"
    - "The five frozen v1 App suites are unmodified across the phase (D-11)"
    - "Zero blackjack files — source OR test — were touched (D-11)"
    - "The STATE blocker ledger is reconciled: WR-01 closed, WR-03 retired, WR-04 closed, CR-02 generation guards not regressed"
    - "No commit anywhere in the phase left a guard suite red — guard amendments landed with the change that required them (Phase 6 commit-discipline lesson)"
    - "A real browser confirms the HE2-01..03 loop, including the A5 pip-clipping check the UI-SPEC flagged as ASSERTED rather than evidenced, and the D-02 toggle-to-fresh-deal moment"
    - "Every source item in the multi-source coverage audit is confirmed shipped, with no silent gaps (GOAL/REQ/RESEARCH/CONTEXT/UI-SPEC)"
  artifacts:
    - path: ".planning/phases/07-2-deck-holdem-evaluation/07-07-SUMMARY.md"
      provides: "The phase's regression record, blocker-ledger reconciliation, coverage audit and human verdict"
      contains: "Multi-Source Coverage Audit"
  key_links:
    - from: "07-07-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "HE2-01..03 confirmed-shipped reconciliation"
      pattern: "HE2-0"
---

## Phase Goal

**As a** probability explorer using the simulator, **I want to** play Hold'em over a 104-card two-deck shoe — with duplicate-card hands evaluated correctly, a Five of a Kind row in the odds table, and a visible cue on the second copy of any card on the felt — **so that** I can see exactly how deck composition changes what hands are possible and how likely they are.

<objective>
Close Phase 7: run the full regression bar, reconcile every source item and every carried blocker against what actually shipped, and take acceptance in a real browser.

Purpose: six plans built this in parallel worktrees. This is the only place the whole thing runs together against the real worker, real Motion and a real browser — the three things every jsdom suite in the phase structurally cannot cover, and the only place the UI-SPEC's one explicitly-unevidenced claim (A5's "the badge never occludes rank or suit pips") can be checked at all. It is also the D-11 gate: six plans' worth of diffs must have left the shipped 1-deck Hold'em path and the entire blackjack surface externally identical.
Output: no source changes. A documented regression record, a blocker-ledger reconciliation, a coverage audit and a verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-2-deck-holdem-evaluation/07-CONTEXT.md
@.planning/phases/07-2-deck-holdem-evaluation/07-UI-SPEC.md
@.planning/phases/07-2-deck-holdem-evaluation/07-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/07-2-deck-holdem-evaluation/07-01-SUMMARY.md
@.planning/phases/07-2-deck-holdem-evaluation/07-02-SUMMARY.md
@.planning/phases/07-2-deck-holdem-evaluation/07-03-SUMMARY.md
@.planning/phases/07-2-deck-holdem-evaluation/07-04-SUMMARY.md
@.planning/phases/07-2-deck-holdem-evaluation/07-05-SUMMARY.md
@.planning/phases/07-2-deck-holdem-evaluation/07-06-SUMMARY.md

<interfaces>
<!-- No new contracts. This plan reads what shipped and verifies it. -->

Reference figures for the reconciliation:
- Pre-phase suite baseline: **52 test files / 697 tests passing** at commit `7d8fb13`.
- D-13 anchors (07-RESEARCH closed forms): conditional hero-`[Ah,Ah]` Five of a Kind rate `93,318 / 83,291,670 = 1.1204e-3`, 3-sigma count band `[179, 269]` at 200,000 trials; marginal uniform-7-of-104 rate `3,354,728 / 21,243,342,120 = 1.5792e-4`, band `[15, 48]` at 200,000 windows.
- Byte-frozen drift detectors that must be green AND unmodified: `src/engine/deckParity.golden.test.ts`, `src/worker/streamingParity.golden.test.ts`, `src/worker/simulationApi.test.ts`, `src/ui/lockedCategory.test.ts`.
- The five frozen v1 App suites: `App.test.tsx`, `App.acceptance.test.tsx`, `App.phase3.acceptance.test.tsx`, `App.modeErrorBanner.test.tsx`, `App.modeSwitchRace.test.tsx`.
- Carried STATE blockers this phase touches: WR-01 (closed early, D-15), WR-03 (retired, D-12), WR-04 (closed, D-07), the Five-of-a-Kind ranking-convention flag (already marked resolved by 07-RESEARCH's pagat.com + Bicycle Cards citations).
</interfaces>

**Executor environment notes (apply to every task in this plan):**
- A fresh worktree needs `npm ci` before anything else runs.
- Never modify `.planning/STATE.md` or `.planning/ROADMAP.md` — the orchestrator owns both.
- Terminate every dev-server and preview process before finishing, including after the checkpoint resolves.
- Commit the SUMMARY before returning.
- This plan changes NO source files. If a gate fails, the fix belongs in the owning plan: record the failure verbatim and route it (07-01 the evaluator, 07-02 stores/picker, 07-03 the category spine and worker, 07-04 the copy cue, 07-05 the toggle/CSS, 07-06 the odds table).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression sweep, blocker-ledger reconciliation and the multi-source coverage audit</name>
  <files>(no source changes — this task produces the SUMMARY's regression record)</files>
  <read_first>
    - All six plan SUMMARYs (the per-plan test-count deltas and every enumerated pre-existing-test adjustment being reconciled)
    - .planning/STATE.md Blockers (the WR-01 / WR-03 / WR-04 entries whose closure is being confirmed, and the CR-02 generation-guard note that must not be regressed)
    - 07-CONTEXT.md D-11 (the 1-deck-unchanged and blackjack-untouched contract)
    - 07-UI-SPEC.md "Isolation Contract" (the three binding rules, all three of which this task verifies mechanically)
  </read_first>
  <action>
    Run the three gates in order and capture exact figures: `npx vitest run` (test-file count, test count, failures, skipped), `npm run lint`, `npm run build`.

    Reconcile the test count against the 52-file / 697-test baseline. The expected shape is 697 pre-existing tests still passing PLUS every case the six plans added. If the pre-existing 697 is not intact, do NOT adjust a pre-existing test to make it pass — treat it as a real regression and route the fix to the owning plan.

    Enumerate every adjustment made to any pre-existing test file across the whole phase, pulling from the six SUMMARYs and confirming each against `git diff` over the phase's commit range. The expected set is exactly six, all mechanical or mandated:
    1. `src/state/gameStore.test.ts` — `deckCount: 1` in the `beforeEach` reset, plus the additive `setDeckCount` describe block (plan 07-02).
    2. `src/state/pickerStore.test.ts` — the additive `hasDuplicatePick` describe block (plan 07-02).
    3. `src/ui/CardPicker.test.tsx` — the `useGameStore` reset in `beforeEach`, plus the additive 2-deck describe block (plan 07-02).
    4. `src/worker/deckCountValidation.test.ts` — the D-12 retarget of the WR-03 acceptance test and the preserved malformed-length rejection (plan 07-03).
    5. `src/engine/shoePath.guard.test.ts` — the additive WR-04 and allowlist blocks plus the one-line `noSetFiles` extension (plan 07-03).
    6. `src/App.modeShell.guard.test.ts` — the deckCount-sweep retarget and the additive Phase 7 style-contract block (plan 07-05).
    Plus one non-test support file: `src/test/holdemTestids.ts` gaining exactly three toggle testids (plan 07-05).
    Any adjustment beyond these must be justified in the SUMMARY as mechanical or treated as a regression. A change that relaxes an assertion, deletes a case, or adds `.skip`/`.todo` is NOT mechanical and is not permitted.

    Confirm the D-11 gates explicitly:
    - `git diff --stat <phase-base>..HEAD -- src/engine/deckParity.golden.test.ts src/worker/streamingParity.golden.test.ts src/worker/simulationApi.test.ts src/ui/lockedCategory.test.ts` must be EMPTY, and all four must be green.
    - The five frozen v1 App suites must be unmodified across the phase.
    - `git diff --stat <phase-base>..HEAD -- $(git ls-files 'src/**/*blackjack*' 'src/**/*Blackjack*')` must be EMPTY — zero blackjack files, source or test, were touched.
    - `src/index.css` must be unmodified, and `git diff <phase-base>..HEAD -- src/App.css` must contain no removed declaration line inside a pre-existing rule body.
    - `src/engine/conditioning.ts` must be unmodified — 07-RESEARCH section 6 established it needs no change, and its `deckCount: DeckCount = 1` default is load-bearing for the deck-parity golden.

    Confirm the structural invariants:
    - `git grep -n "deckCount" -- src/App.tsx src/state/gameModeStore.ts src/ui/GameModeSwitcher.tsx` returns nothing.
    - `git grep -in "blackjack\|gamemode" -- src/state/gameStore.ts src/state/oddsStore.ts src/state/pickerStore.ts src/state/uiStore.ts src/engine/conditioning.ts` returns nothing.
    - The `evaluateHand(` call-site allowlist and the `@poker-apprentice/hand-evaluator` import allowlist from plan 07-03 are green.
    - `git grep -n "cancelSimulation(" -- src/ui/HoldemGame.tsx` shows exactly one non-comment call site.
    - The CR-02 generation guards are not regressed: `git grep -n "dealNonce" -- src/ui/HoldemGame.tsx` still shows the same-key/wrong-generation check, and the blackjack equivalent is untouched by construction.

    Reconcile the STATE blocker ledger, recording evidence for each:
    - **WR-01 (closed early this phase, D-15):** `src/ui/CardPicker.tsx` reads a live deckCount and passes it to `setPick`; a behavioral test asserts the second copy actually reaches the store.
    - **WR-03 (retired this phase, D-12):** `src/worker/deckCountValidation.test.ts` now runs a real end-to-end 2-deck poker simulation; `ui/HoldemGame.tsx` left the modeShell deckCount sweep with the rationale retargeted, and the three shell files still carry it.
    - **WR-04 (closed this phase, D-07):** the `.includes(` prohibition covers all five shoe-path files; behavioral 2-deck picker tests exist.
    - **Five-of-a-Kind ranking-convention flag:** already resolved by 07-RESEARCH; confirm the shipped comparator implements "above Royal Flush, ties by rank" and that the module cites the convention as a working convention.
    - **06-REVIEW WR-01 leak-acceptance precedent:** note whether Phase 7's A4 guard has the same one-bit-leak shape. It does NOT — Hold'em's guard reads the PICKS, which are user-authored and fully visible, not any hidden card. Record that distinction so Phase 8 does not carry a nonexistent precedent forward.

    Write the **Multi-Source Coverage Audit** into the SUMMARY, one row per source item, each marked COVERED with the plan that shipped it and the evidence:
    - **GOAL** — ROADMAP Phase 7's goal and its four success criteria.
    - **REQ** — HE2-01, HE2-02, HE2-03.
    - **RESEARCH** — the stamped-array gate; the Five-of-a-Kind branch; the suit-remap proxy and its step-5 exactness assertion; the one-suit flush scorer and the max-with-proxy step; the extended comparator; the brute-force oracle methodology reproduced as committed tests; both probability anchors; the hoisted evaluator selection; grow-on-merge; the odds-store guard widening; the `lockedInCategory` routing; the copy-cue canonical scan order; the 12-point integration map; the Pitfall-7 correction (VALUE-asserting tests, never mere non-throwing); Pitfalls 1-8; Assumptions A1, A2 and A3.
    - **CONTEXT** — D-01 through D-16, each with the plan that implemented it.
    - **UI-SPEC** — A1 through A12, the seven new testids, the Copywriting Contract's every locked string, the Accessibility Contract, and all three Isolation Contract rules.
    If ANY item is not covered, stop and report `## ⚠ Source Audit: Unplanned Items Found` rather than closing the phase.

    Verify per-commit guard health across the phase's whole range: for each commit that touches `src/App.modeShell.guard.test.ts` or `src/engine/shoePath.guard.test.ts`, confirm that guard suite was green AT that commit. The Phase 6 lesson is that a guard amendment must ride the commit that requires it; a red-then-fixed pair is a finding, not a pass.

    Confirm no dev-server, preview or watch process is running before handing off to the checkpoint.
  </action>
  <acceptance_criteria>
    - Test command: `npx vitest run` exits 0 with 0 failures, 0 skipped, and a test count of at least 697 plus the phase's additions.
    - Test command: `npm run lint` exits 0 with no new inline ESLint disable comments introduced anywhere in the phase.
    - Test command: `npm run build` exits 0.
    - Behavior: the four byte-frozen drift detectors produce empty `git diff --stat` output over the phase range and are green.
    - Behavior: the five frozen v1 App suites are unmodified.
    - Behavior: the blackjack file sweep produces empty `git diff --stat` output — zero blackjack files touched.
    - Behavior: `src/index.css` and `src/engine/conditioning.ts` are unmodified.
    - Behavior: every structural grep listed in the action returns the stated result.
    - Behavior: each commit touching a guard suite left that suite green at that commit.
    - Source: the SUMMARY contains the Multi-Source Coverage Audit with every GOAL/REQ/RESEARCH/CONTEXT/UI-SPEC item marked COVERED and attributed to a plan.
    - Source: the SUMMARY contains the blocker-ledger reconciliation with evidence for WR-01, WR-03, WR-04 and the ranking-convention flag.
    - Source: the SUMMARY lists every pre-existing-test adjustment with its justification.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>All three gates are green, the 697-test baseline is intact, every pre-existing-test adjustment is enumerated and justified, every drift detector is byte-untouched, the blocker ledger is reconciled with evidence, and every source item is confirmed shipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser acceptance of 2-deck Hold'em (HE2-01..03)</name>
  <files>(no source changes — this is a blocking verification of the running app)</files>
  <read_first>
    - 07-UI-SPEC.md "Interaction States &amp; Testid Contract", "Copywriting Contract", "Accessibility Contract" and "Isolation Contract" (the contract being accepted)
    - 07-UI-SPEC.md A5 (the pip-clipping check is explicitly ASSERTED, not evidenced — this checkpoint is the only place it can be verified) and A3 (the honesty affordances being checked)
    - 07-CONTEXT.md `<specifics>` (the Five of a Kind row's convergence from 0.00% to a small nonzero value IS the phase's visible payoff; the first duplicate must read as "two physical copies" instantly)
    - 07-RESEARCH.md Pitfall 2 (dev-console `[oddsStore consistency guard]` spam is a real failure signal for step 5)
  </read_first>
  <action>
    Present the verification script below and BLOCK. Do not proceed, do not mark the phase complete, and do not auto-approve — this checkpoint is never auto-advanceable regardless of workflow settings.

    Run BOTH surfaces, because they check different things:
    - `npm run dev` for the dev-console checks (the odds-store consistency guard is DEV-only and cannot fire in a production build).
    - `npm run build && npm run preview -- --port 4319` for the production check. Use port 4319 deliberately: do NOT reuse port 5199 or any port a dev server in this session may already hold, so a stale server cannot be mistaken for the fresh build.
    Report both URLs, and stop both processes once the verdict is in.

    Under the standing autonomy directive, an agent MAY execute these steps directly in a browser and record the observed result as the verdict, clearly labelled agent-executed rather than developer-confirmed. Every step below is written to be FRAME-INDEPENDENT — each check compares observable end states (text content, element present or absent, enabled or disabled, a number before versus after) rather than judging an animation mid-flight — so an agent driving the browser can evaluate it without catching a specific frame.

    Two steps (7 and 11) are the exceptions that require observing something DURING motion. Fallback for those: suspend the frame instead of racing it. Open DevTools, use the Animations panel to set playback speed to 10% (or 25%), then perform the action and read the resulting still state. If the agent cannot reliably slow or capture the animation, it must record those steps as NOT VERIFIED rather than assumed-pass, and the phase closes with that gap named explicitly in the SUMMARY.

    If any step fails, record it verbatim in the SUMMARY and route the fix to its owning plan rather than patching around it here.
  </action>
  <what-built>
    2-deck Hold'em end to end. A Hold'em-local `1 deck` / `2 decks` segmented toggle in the control bar; switching it with a hand on the table deals a fresh hand from a 104-card shoe. Hands containing two physical copies of the same card evaluate correctly through a duplicate-detection gate, a suit-remap proxy and a custom flush scorer rather than the stock evaluator, which returns silent garbage on that input. Five of a Kind appears as an eleventh odds-table row at the strength end, with a live-converging probability and a working locked-in tick. The second visible copy of any duplicated card on the felt wears an `×2` corner badge that rides the card through every animation. The card picker honours two copies per value with count-aware titles. Automated coverage: a duplicate-evaluation vector suite, a brute-force-oracle property suite, two seeded closed-form frequency anchors, engine and worker integration suites, a copy-cue derivation and render suite, an odds-table both-ways DOM-absence suite, an App-level toggle-and-cache suite, and three extended source-shape guards — on top of the intact 697-test baseline.
  </what-built>
  <how-to-verify>
    Start the dev server with `npm run dev` and open the printed local URL.

    1. **Hold'em at one deck is unharmed (D-11).** Before touching the toggle: click `Deal`, watch the odds converge, click `Advance` through flop, turn and river, `Rewind` back, reveal an opponent seat, open `Set Up Scenario` and pick a card. Confirm the category table has exactly ten rows ending in `Royal Flush`, with no `Five of a Kind` row anywhere and no `×2` badge on any card. Everything must behave exactly as it did before this phase. Any difference here is a FAIL, not a cosmetic note.

    2. **Blackjack is untouched (D-11).** Switch to Blackjack, deal a round, toggle its deck control, switch back to Hold'em. Nothing about that game may look or behave differently.

    3. **The toggle exists where it should (A2).** In Hold'em mode confirm the control bar reads, left to right: the game-mode switcher, `Deal`, `Set Up Scenario`, the street controls, then a `1 deck` / `2 decks` segmented control as the LAST child. It must look identical in treatment to the other game's deck toggle and to the mode switcher — same border, same active-segment fill, no purple accent anywhere on it.

    4. **Idle switch is silent (D-02).** With no hand on the table (reload if needed), click `2 decks`. The active segment moves and NOTHING else changes: no cards, no odds activity, no trial counter movement. Hover both segments — neither shows a tooltip.

    5. **THE PAYOFF: Five of a Kind converges (HE2-02, the headline check).** Click `Deal` at 2 decks. Confirm the category table now has ELEVEN rows, with `Five of a Kind` as the LAST row immediately after `Royal Flush`. Watch it: it should start at an em dash, then read `0.00%` and — over a few deals — occasionally tick to a small nonzero value. Then use `Set Up Scenario` to pick the SAME card into both hero slots (for example the Ace of Hearts twice; the second pick must be allowed, and only a third attempt at that card blocked), deal, and advance to the river. The `Five of a Kind` row must now show a visibly nonzero percentage. **If the row does not exist, or its number never leaves 0.00% even with a duplicated pocket pair, this step FAILS** — that number is the whole point of the phase. Also confirm the table's caption and the sentence under it read exactly as they do at one deck, with no deck-count wording added.

    6. **The locked-in tick reaches the new row.** Continue the hand above until the visible cards actually make five of a kind (a duplicated pocket pair plus three more of that rank on the board — construct it with the picker if needed). Confirm the tick appears on the `Five of a Kind` row and on no other row.

    7. **THE FIRST DUPLICATE READS AS A FEATURE (HE2-03).** With two identical cards visible, confirm exactly ONE of them wears a small dark `×2` badge in its top-right corner, and the first copy wears none. The badge must sit INSIDE the card's bounds, use the dark green felt colour with white text, and never be red or carry a warning symbol. **A5 pip-clipping check (the UI-SPEC flags this as asserted, not evidenced — this is its only verification):** zoom in and confirm the badge does not cover or clip any rank character or suit pip, at the hero card width AND at the smaller opponent card width after revealing an opponent holding a duplicate. Then click `Deal` again and watch the badge during the fly-in — it must travel WITH its card, never lag behind or sit still in the slot. Use the DevTools Animations panel at 10% speed if the motion is too fast to judge; if you cannot slow it, record this half as NOT VERIFIED.

    8. **The badge follows knowledge, not chronology.** Deal a 2-deck hand where a hero card's twin lands on the turn or river (use the picker). At preflop, no badge. Advance to the street that boards the twin: the badge appears on the board card. `Rewind` one street: the badge disappears. Advance again: it comes back on the same card, never on the hero card instead.

    9. **THE FRESH-DEAL TOGGLE (D-02/A3).** With a settled hand on the table, hover the INACTIVE segment and confirm the tooltip reads exactly `Switching the shoe deals a fresh hand`. Hover the ACTIVE segment: no such tooltip. Now click the inactive segment. Confirm, in order: the old cards vanish instantly with no exit animation; a completely new hand deals in from the deck; the odds panel blanks to em dashes in the same instant; the trial counter restarts from zero and climbs; and the street indicator is back at preflop. Then click the ALREADY-ACTIVE segment: absolutely nothing happens — no re-deal, no counter reset.

    10. **The 2-to-1 guard never eats a pick (A4).** Open `Set Up Scenario` at 2 decks and pick the same card into two hero slots. Confirm `1 deck` is now dimmed and not clickable, with the tooltip `Your picked cards include a duplicate — impossible with one deck`. Confirm `2 decks` is unaffected. Clear one of the two picks: `1 deck` becomes clickable again in the same instant, and the OTHER pick is still there. At no point may a pick disappear on its own.

    11. **Picker copy states (A9).** At 2 decks, open the picker with one copy of a card already picked elsewhere. That card's cell must still be clickable, still read just the card code with no `(used)` suffix, and show the tooltip `1 of 2 copies used`. After both copies are picked it must be dimmed, read `{card} (used)`, and show `Both copies already used in this hand`. Switch to 1 deck (clear duplicates first) and confirm the tooltip is back to the shipped `Already used in this hand`.

    12. **Street navigation and reveal at 2 decks (HE2-01).** With a 2-deck hand, advance through every street, rewind, and reveal all three opponents one at a time. The odds must recondition on each change and settle. If any opponent's revealed hand contains a duplicate of a visible card, the badge must appear on the later-in-scan copy.

    13. **Console hygiene (07-RESEARCH Pitfall 2).** With DevTools open on the DEV server, repeat a 2-deck deal, a street advance and a reveal. The console must be clean — in particular there must be ZERO `[oddsStore consistency guard]` messages. Any such message is a FAIL: the guard is report-only, and one that fires on every legitimate 2-deck snapshot trains people to ignore it.

    14. **Keyboard and focus.** Tab to the deck toggle and activate a segment with Enter or Space. Confirm a visible focus ring, that the active segment is never greyed out under normal play, that focus stays on the clicked segment across the fresh deal, and that the app is never keyboard-trapped.

    15. **Screen-reader delivery (A11).** With a duplicate visible, inspect the badged card in the accessibility tree: the `×2` glyph itself must be hidden from assistive technology, with the sentence `Second copy — two physical copies of this card are in play` present instead. Reveal an opponent holding a badged card and confirm that seat's button name ends with ` — second copy of {card}`.

    16. **Reduced motion.** Enable your OS "reduce motion" setting and reload. Deal, toggle the deck count, advance and reveal at 2 decks. Every card must appear instantly in its final position, every badge must be immediately in place on the correct card, and every number must still converge normally — no functionality may be gated behind an animation actually playing.

    17. **Production build.** Stop the dev server. Run `npm run build && npm run preview -- --port 4319` and open that URL. Repeat steps 5, 7 and 9 against the production bundle. This catches worker-chunk and asset-path breakage that the dev server hides. The console must be clean here too (the dev-only consistency guard will correctly be silent).

    Stop the dev server and the preview server when finished.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 7, or describe exactly which step failed and what you saw.</resume-signal>
  <acceptance_criteria>
    - Behavior: the verifier confirms steps 1-17 pass, or names the exact failing step.
    - Behavior: step 5 shows an eleventh `Five of a Kind` row at the strength end whose percentage goes visibly nonzero with a duplicated pocket pair — no such number is a FAIL under HE2-02.
    - Behavior: step 7 confirms exactly one badge per duplicated pair, no pip clipping at either card width, and the badge travelling with its card.
    - Behavior: step 9 confirms the fresh deal, the counter restart and the preflop reset, plus the exact A3 tooltip text.
    - Behavior: step 10 confirms no pick is ever silently cleared.
    - Behavior: step 13 confirms zero `[oddsStore consistency guard]` console messages at 2 decks.
    - Behavior: steps 1 and 2 confirm the 1-deck Hold'em loop and the entire blackjack surface behave exactly as before — any difference is a FAIL under D-11.
    - Behavior: step 17 confirms the production bundle behaves identically on a port distinct from any dev server.
    - Source: the verdict and every observation are recorded verbatim in `07-07-SUMMARY.md`, labelled developer-confirmed or agent-executed, with any NOT VERIFIED step named explicitly.
    - Behavior: no dev-server or preview process remains running after the checkpoint resolves.
  </acceptance_criteria>
  <verify>
    <human-check>Verifier confirms in a real browser: the 1-deck Hold'em loop and the whole blackjack surface are unchanged; the deck toggle sits last in the control bar and matches the shipped segmented-control treatment; an idle switch is silent and a mid-hand switch deals a fresh hand with the exact A3 tooltip; the Five of a Kind row exists only at 2 decks, sits last, and converges to a visibly nonzero number with a duplicated pocket pair; the locked-in tick reaches it; the second copy of a duplicated card wears an x2 badge that clips no pip at either card width and rides its card through the fly-in; the badge tracks street knowledge and disappears on rewind; the 2-to-1 guard explains itself and never eats a pick; the picker's count-aware titles are correct at both deck counts; street navigation and reveal work at 2 decks; the console is clean with zero odds-store consistency-guard messages; keyboard and screen-reader delivery match the contract; reduced motion gates nothing; and the production build on port 4319 behaves identically.</human-check>
    <automated>npx vitest run &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>The verifier has approved (or every reported failure has been fixed in its owning plan and re-verified), the verdict and its provenance are recorded in the SUMMARY, and no server process is left running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main thread -> real Web Worker | Every automated suite in the phase mocks the simulation service. Steps 5, 9, 12 and 17 are the only exercise of the real Comlink boundary carrying a `deckCount: 2` payload and an 11-entry snapshot back. |
| real Motion -> the copy-cue badge | jsdom forces reduced motion, so every duration is 0 there. Step 7 is the only place the badge is observed riding a real, non-zero fly-in — the D-08 "rides the card, not the slot" claim. |
| vendored card SVG art -> badge placement | Step 7's pip-clipping check. UI-SPEC A5 explicitly labels the no-occlusion claim ASSERTED rather than evidenced against the actual SVGs; nothing automated can check it. |
| dev-only guards -> production | The odds-store consistency guard exists only in DEV, so step 13 must run on the dev server and step 17 must run on the production build; neither substitutes for the other. |
| shipped 1-deck behavior -> six plans' diffs | Step 1 is the human-observable half of the D-11 contract that the byte-frozen goldens cover mechanically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-35 | Tampering | shipped 1-deck Hold'em across six parallel diffs | mitigate | Task 1's byte-untouched golden and frozen-suite gates plus step 1's human walk of the full 1-deck loop — two independent detectors for the same contract. |
| T-07-36 | Tampering | the blackjack surface | mitigate | A `git diff --stat` sweep over every blackjack-named file plus step 2's live walk; the CSS isolation is separately pinned by plan 07-05's source-level guard. |
| T-07-37 | Repudiation | the claim that Five of a Kind has a correct probability | mitigate | Two seeded closed-form anchors in plan 07-01, an integration reconciliation in plan 07-03, and step 5's requirement that the displayed number actually go nonzero under a constructed duplicated pocket pair. |
| T-07-38 | Repudiation | A5's unevidenced no-pip-clipping claim | mitigate | Step 7 verifies it against the real vendored SVGs at both card widths. It is the only possible check, and it is why this checkpoint cannot be skipped. |
| T-07-39 | Denial of Service | dev-guard console spam at 2 decks | mitigate | Step 13 runs on the DEV server specifically, because the guard cannot fire in a production build. |
| T-07-40 | Denial of Service | worker-chunk or asset breakage visible only in a production bundle | mitigate | Step 17 repeats the three load-bearing steps against `npm run preview` on a deliberately distinct port, so a stale dev server cannot be mistaken for the fresh build. |
| T-07-41 | Repudiation | agent-executed verification passed off as human sign-off | mitigate | The verdict must be labelled developer-confirmed or agent-executed, and any step an agent cannot reliably observe (7 and 11's motion halves) must be recorded NOT VERIFIED rather than assumed-pass. |
| T-07-42 | Tampering | regression-bar erosion across six plans | mitigate | Task 1 forbids relaxed assertions, deleted cases and `.skip`/`.todo`, enumerates the exactly-six permitted pre-existing-test adjustments, requires per-commit guard health, and requires the coverage audit to name a plan for every source item. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs and zero new static assets this phase. `npm ci` from the committed lockfile only. An executor reaching for `npm install <package>` must stop and escalate. |
</threat_model>

<verification>
1. `npx vitest run` — 0 failures, 0 skipped, count at least 697 plus the phase's additions.
2. `npm run lint` — exits 0.
3. `npm run build` — exits 0.
4. The four byte-frozen drift detectors and the five frozen v1 App suites are green AND unmodified across the phase range.
5. Zero blackjack files touched; `src/index.css` and `src/engine/conditioning.ts` unmodified.
6. Every structural grep in Task 1 returns its stated result.
7. Multi-Source Coverage Audit complete with zero MISSING rows.
8. Blocker ledger reconciled with evidence for WR-01, WR-03, WR-04 and the ranking-convention flag.
9. Checkpoint returns "approved", with provenance labelled and any NOT VERIFIED step named.
10. No dev-server or preview process left running.
</verification>

<success_criteria>
- The full suite is green with the 697-test baseline intact and every pre-existing-test adjustment enumerated as mechanical or explicitly mandated (D-11).
- Every drift detector passed byte-untouched through six plans' worth of diffs, and the 1-deck Hold'em loop plus the entire blackjack surface are confirmed unchanged in a browser (D-11).
- The carried blocker ledger is reconciled: WR-01 closed, WR-03 retired, WR-04 closed, the ranking-convention flag resolved, CR-02 guards intact.
- A real browser confirms HE2-01, HE2-02 and HE2-03, with the Five of a Kind row going visibly nonzero and the copy cue clipping no pips at either card width.
- Every GOAL / REQ / RESEARCH / CONTEXT / UI-SPEC source item is confirmed shipped and attributed to a plan, with no silent gaps.
- No commit in the phase left a guard suite red.
</success_criteria>

<output>
Create `.planning/phases/07-2-deck-holdem-evaluation/07-07-SUMMARY.md` when done.

Record in the SUMMARY: the final test-file and test counts; the full enumeration of pre-existing-test adjustments with justifications; the drift-detector and blackjack-isolation confirmations; the per-commit guard-health result; the complete blocker-ledger reconciliation; the complete Multi-Source Coverage Audit table; the checkpoint verdict with its provenance label and any NOT VERIFIED steps; and any observation the verifier raised that was routed to a follow-up rather than fixed in-phase.
</output>
