---
phase: 05-game-mode-shell-store-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.modeSwitch.test.tsx
  - src/state/gameModeStore.ts
  - src/state/gameModeStore.test.ts
  - src/ui/GameModeSwitcher.tsx
  - src/ui/GameModeSwitcher.test.tsx
  - src/ui/BlackjackScene.tsx
  - src/App.tsx
  - src/App.css
autonomous: true
requirements: [BJ-01]

must_haves:
  truths:
    - "User sees a segmented two-button control labeled exactly \"Hold'em\" and \"Blackjack\" in the top control bar, visible in BOTH modes, with the active game indicated by a non-accent treatment (D-01)"
    - "Mode lives only in a new src/state/gameModeStore.ts holding exactly { mode, setMode } — no URL routing, no deckCount field (D-01, D-02)"
    - "Switching to Blackjack shows a felt-shell scene containing only an honest empty-state block naming Phase 6's deliverables, with zero interactive gameplay controls (D-03)"
    - "While mode is blackjack, NO Hold'em testid and no Deal button exists anywhere in the DOM — the Hold'em subtree is unmounted, not hidden (D-04)"
    - "The App.tsx odds effect returns early when mode !== 'holdem' and lists mode in its dependency array, so no simulation starts in blackjack mode (D-05)"
    - "Zero changes to the SHAPES of gameStore/oddsStore/pickerStore/uiStore — they stay Hold'em-owned and unaware Blackjack exists (D-05)"
    - "Leaving Hold'em rides the effect's EXISTING ignore-flag cleanup for cancellation — no second cancelSimulation() call site is added (D-07)"
    - "No production code calls useUiStore.resetAnimations(); the gate drains solely via the existing useAnimationGate/useExitGate unmount cleanups (D-07, D-08)"
    - "Nothing in this plan touches the deckCount wire path and no file passes deckCount:2 (D-10, WR-02, WR-03)"
  artifacts:
    - path: "src/state/gameModeStore.ts"
      provides: "The only cross-game store: mode literal union + setMode"
      contains: "useGameModeStore"
    - path: "src/ui/GameModeSwitcher.tsx"
      provides: "Segmented two-button mode control, role=group, aria-pressed"
      contains: "game-mode-switcher"
    - path: "src/ui/BlackjackScene.tsx"
      provides: "Felt-shell placeholder with the locked blackjack-empty-state copy"
      contains: "blackjack-empty-state"
    - path: "src/App.modeSwitch.test.tsx"
      provides: "End-to-end happy-path proof of the switch in both directions"
    - path: "src/App.tsx"
      provides: "Mode-scoped odds effect + conditional render tree fork"
      contains: "useGameModeStore"
  key_links:
    - from: "src/App.tsx"
      to: "src/state/gameModeStore.ts"
      via: "useGameModeStore selector subscribed at component top-level, mode added to the effect dependency array"
      pattern: "useGameModeStore\\(\\(state\\) => state\\.mode\\)"
    - from: "src/ui/GameModeSwitcher.tsx"
      to: "src/state/gameModeStore.ts"
      via: "setMode bound to each button's onClick"
      pattern: "setMode\\('(holdem|blackjack)'\\)"
    - from: "src/App.tsx"
      to: "src/ui/BlackjackScene.tsx"
      via: "mode === 'blackjack' conditional render"
      pattern: "mode === 'blackjack'"
---

## Phase Goal

**As a** probability explorer using the simulator, **I want to** switch between Hold'em and Blackjack via an on-screen mode switcher, **so that** each game keeps fully independent state and odds and neither leaks into or corrupts the other.

<objective>
Deliver the whole user-visible slice of BJ-01 in one plan: a working mode switch in both directions, with the Hold'em subtree unmounted in Blackjack mode and an honest Blackjack placeholder in its place.

Purpose: This is the phase's genuine vertical slice — after this plan a real user can do something they could not do before (open a second game). Plan 02 proves the isolation is real; Plan 03 gates the regression bar and accepts it in a browser.
Output: `src/state/gameModeStore.ts`, `src/ui/GameModeSwitcher.tsx`, `src/ui/BlackjackScene.tsx`, a mode-forked `src/App.tsx`, new CSS rules, and a passing end-to-end switch test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-game-mode-shell-store-separation/05-CONTEXT.md
@.planning/phases/05-game-mode-shell-store-separation/05-UI-SPEC.md
@.planning/phases/05-game-mode-shell-store-separation/05-PATTERNS.md

@src/App.tsx
@src/state/uiStore.ts
@src/ui/StreetControls.tsx

**TESTID CONFLICT — UI-SPEC WINS.** `05-PATTERNS.md` line 112 and its "Shared Patterns" section propose `mode-switch-holdem` / `mode-switch-blackjack`. Those names are STALE. `05-UI-SPEC.md` is the APPROVED contract and locks `game-mode-switch-holdem` / `game-mode-switch-blackjack` / `game-mode-switcher`. Use the UI-SPEC names everywhere. PATTERNS is authoritative for *shape* (which analog file to copy), UI-SPEC is authoritative for *strings* (testids, copy, tokens).

<interfaces>
<!-- The new contract this phase introduces. Downstream (Plan 02, Plan 03, Phase 6) consume it directly — no codebase exploration needed. -->

New — `src/state/gameModeStore.ts`:
```typescript
export type GameMode = 'holdem' | 'blackjack';

interface GameModeState {
  mode: GameMode;
  setMode: (mode: GameMode) => void;
}

export const useGameModeStore: UseBoundStore<StoreApi<GameModeState>>;
```

Existing, consumed unchanged — `src/state/uiStore.ts`:
```typescript
pendingAnimationCount: number;   // gate counter; resetAnimations() is TEST-ONLY, never called from production
```

Existing, consumed unchanged — `src/state/simulationService.ts`:
```typescript
export function startSimulation(
  conditioned: ConditionedState,
  onProgress: (snapshot: ProgressSnapshot) => void,
  onError: (message: string) => void,
): Promise<void>;
export function cancelSimulation(): Promise<void>;   // exactly ONE call site allowed: App.tsx's effect cleanup
```

Existing CSS custom properties available (`src/index.css`, verified present in both light and dark blocks):
`--border`, `--text`, `--text-h`, `--felt`, `--felt-dark`, `--felt-rail`, `--seat-badge-text`
</interfaces>

Baseline pinned before any edit: `npx vitest run` → **29 test files, 281 tests, all passing** (measured 2026-08-24). Any deviation from 281+N at the end of this plan must be justified in the SUMMARY per D-09.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — end-to-end mode-switch happy path</name>
  <files>src/App.modeSwitch.test.tsx</files>
  <read_first>
    - src/App.tsx (the tree under test; the control bar and JSX body this test will fork)
    - src/App.test.tsx lines 1-45 (the `vi.mock('./state/simulationService')` explicit-factory + `resetStores()` harness to copy verbatim)
    - .planning/phases/05-game-mode-shell-store-separation/05-UI-SPEC.md sections "Testid Contract" and "Copywriting Contract" (the exact strings this test pins)
  </read_first>
  <behavior>
    - Default mode is Hold'em: `game-mode-switcher` exists; `game-mode-switch-holdem` has `aria-pressed="true"`; `game-mode-switch-blackjack` has `aria-pressed="false"`; `table-scene` and `odds-panel` are in the document; `blackjack-scene` is NOT.
    - The switcher wrapper carries `role="group"` and `aria-label="Game mode"`; neither button is `disabled` in either mode (UI-SPEC A4/A5).
    - Clicking the Blackjack button mounts `blackjack-scene` and `blackjack-empty-state`, whose `<h2>` text is exactly `The Blackjack table deals next` and whose `<p>` text is exactly `Player hand, dealer upcard, live bust and outcome odds, and Stand-vs-Hit choices land here next. Switch back to Hold'em to keep watching odds converge now.`
    - In Blackjack mode, EVERY Hold'em testid is absent (assert via `it.each` over the list: `table-scene`, `odds-panel`, `hero-hole`, `opponents`, `board-cards`, `street-label`, `rewind-button`, `advance-button`, `set-up-scenario-button`, `empty-hand-state`, `trial-counter`, `card-picker`) AND `screen.queryByRole('button', { name: /^deal$/i })` is null (the Deal button has no testid — UI-SPEC Testid Contract calls this out explicitly).
    - The `blackjack-scene` subtree contains ZERO `<button>` elements (D-03: no dead or disabled gameplay controls).
    - Clicking the Hold'em button restores the Hold'em tree (`table-scene` and `odds-panel` back in the document) and unmounts `blackjack-scene`; `aria-pressed` flips back.
    - `game-mode-switcher` and both switch buttons remain in the document in BOTH modes (D-01 "visible in both modes").
  </behavior>
  <action>
    Create `src/App.modeSwitch.test.tsx` as a new sibling to `App.test.tsx` — do NOT edit `App.test.tsx`, `App.acceptance.test.tsx`, or `App.phase3.acceptance.test.tsx` in this task (D-09 keeps the v1 acceptance suites as the untouched regression harness).

    Copy the explicit `vi.mock('./state/simulationService', () => ({ startSimulation: vi.fn(), cancelSimulation: vi.fn() }))` factory and the `resetStores()` helper from `App.test.tsx` lines 19-34 verbatim (the factory is mandatory — bare automocking imports the real module, which instantiates a real `Worker` at module scope and jsdom cannot do that). Extend `resetStores()` with `useGameModeStore.setState({ mode: 'holdem' })` so every test starts in the default mode, mirroring how `gameStore` is reset via `.setState` rather than a bespoke production reset.

    Drive interactions with `userEvent.setup()` + `user.click(screen.getByTestId('game-mode-switch-blackjack'))`, matching the `set-up-scenario-button` toggle test at `App.test.tsx` lines 444-456.

    Write a top-of-file rationale comment naming the D-numbers this file protects (D-01, D-03, D-04) in the style of `src/App.acceptance.test.tsx` lines 13-19, and title each `describe` block `'<subject> — <invariant> (D-NN)'` per the codebase convention.

    Assert copy with exact-string matchers (`toHaveTextContent` against the full literal, or `toBe` on `textContent`), never a substring or regex — the UI-SPEC Copywriting Contract strings are contractual and a loose matcher would let a reworded placeholder pass.

    Import `useGameModeStore` from `./state/gameModeStore` even though that module does not exist yet — the resulting failure is the RED signal.

    Commit with `test(05-01): add failing end-to-end mode-switch test (D-01/D-03/D-04)`.
  </action>
  <acceptance_criteria>
    - Source: `src/App.modeSwitch.test.tsx` exists and contains the literal string `game-mode-switcher`, `blackjack-empty-state`, and both locked copy strings from UI-SPEC's Copywriting Contract character-for-character.
    - Source: the file contains `vi.mock('./state/simulationService'` with an explicit factory object (not a bare `vi.mock` one-arg call).
    - Source: the file contains `queryByRole('button', { name: /^deal$/i })`.
    - Source: `src/App.test.tsx`, `src/App.acceptance.test.tsx`, and `src/App.phase3.acceptance.test.tsx` are byte-unchanged (`git diff --stat` lists none of them).
    - Behavior: the new test FAILS, and the failure output mentions `game-mode-switcher` or `gameModeStore` — proving it fails on the missing feature, not on a syntax or harness error.
    - Behavior: `npx vitest run --exclude 'src/App.modeSwitch.test.tsx'` still reports 281 passing tests.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/App.modeSwitch.test.tsx 2>&1 | grep -qE "game-mode-switcher|gameModeStore" &amp;&amp; echo "RED CONFIRMED"</automated>
  </verify>
  <done>The end-to-end switch test exists, fails for the right reason, and no pre-existing test file was edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN part A — gameModeStore and the GameModeSwitcher control</name>
  <files>src/state/gameModeStore.ts, src/state/gameModeStore.test.ts, src/ui/GameModeSwitcher.tsx, src/ui/GameModeSwitcher.test.tsx, src/App.css</files>
  <read_first>
    - src/state/uiStore.ts (the exact minimal-curried-store shape and doc-comment density to copy — this is the analog, NOT gameStore.ts)
    - src/state/oddsStore.ts lines 6-7 (the one-line "must not import <other store>" guard comment convention)
    - src/state/uiStore.test.ts (the direct-`getState()`, no-React store test shape)
    - src/ui/StreetControls.tsx (the thin two-sibling-button control component shape to copy)
    - src/App.css lines 501-522 (the existing `.control-bar` / `.control-bar > button` rules the switcher sits beside) and lines 332-343 (the phase-tagged CSS banner-comment convention)
    - .planning/phases/05-game-mode-shell-store-separation/05-UI-SPEC.md section "Mode Switcher" (the contractual CSS block and label strings)
  </read_first>
  <behavior>
    - `useGameModeStore.getState().mode` is `'holdem'` on first read (default mode).
    - `setMode('blackjack')` then `getState().mode === 'blackjack'`; `setMode('holdem')` returns it.
    - Calling `setMode` with the value already in state is a harmless no-op that leaves `mode` unchanged (UI-SPEC A5: clicking the active button must not be an error path).
    - `GameModeSwitcher` rendered standalone shows two buttons named exactly `Hold'em` and `Blackjack`, inside a `role="group"` / `aria-label="Game mode"` wrapper testid'd `game-mode-switcher`.
    - The button matching the current mode has `aria-pressed="true"`, the other `aria-pressed="false"`; clicking the inactive one flips both attributes AND writes `useGameModeStore.getState().mode`.
    - Neither button is ever `disabled`, in either mode.
    - Button label text NEVER changes with state — `aria-pressed` carries the state, mirroring the `Set Up Scenario` toggle's locked-label rule.
  </behavior>
  <action>
    Create `src/state/gameModeStore.ts` following `uiStore.ts`'s exact shape: `create<GameModeState>()((set) => ({ ... }))`. Export a `GameMode` type as a literal union `'holdem' | 'blackjack'` (a literal union, never a boolean and never a TS `enum` — mirrors `Street` in `src/engine/streets.ts` and `DeckCount` in `src/engine/shoe.ts`). State is EXACTLY `{ mode, setMode }` per D-02: do NOT pre-add `deckCount` — that field is Phase 6/Phase 8's, and adding it here would trip the WR-02/WR-03 traps (D-10). Add a load-bearing doc comment on `setMode` and a one-line file-level guard comment, modeled on `oddsStore.ts` line 6, stating that this module must not import `gameStore`/`oddsStore`/`pickerStore`/`uiStore` — those stores stay entirely unaware Blackjack exists (D-05). Tag comments with the D-numbers they trace to, matching the codebase's citation density.

    Create `src/state/gameModeStore.test.ts` in `uiStore.test.ts`'s shape: `beforeEach` resets via `useGameModeStore.setState({ mode: 'holdem' })`, then plain `getState()` assertions with no `render()` and no React import. Title the describe block `gameModeStore — mode is the only cross-game field (D-02)`.

    Create `src/ui/GameModeSwitcher.tsx` copying `StreetControls.tsx`'s structure: two `useGameModeStore` selectors (`mode`, `setMode`), a wrapper `<div data-testid="game-mode-switcher" role="group" aria-label="Game mode">`, and two `<button type="button">` children with `data-testid="game-mode-switch-holdem"` and `data-testid="game-mode-switch-blackjack"`, each carrying `aria-pressed={mode === '<its own mode>'}` and `onClick={() => setMode('<its own mode>')}`. Label text is the literal `Hold'em` and `Blackjack` (D-01, locked verbatim — no ellipsis, no icon, no case change, no dynamic rewriting). Plain `Hold'em` as JSX text is safe here: this repo's flat ESLint config extends only `js.configs.recommended`, `tseslint.configs.recommended`, `react-hooks`, and `react-refresh` — `eslint-plugin-react` (and therefore `react/no-unescaped-entities`) is not installed, so no `&apos;` escape is needed and the source stays greppable for the literal label. Do NOT export any non-component binding from this file (`react-refresh/only-export-components` is active). Read no store other than `gameModeStore`.

    Create `src/ui/GameModeSwitcher.test.tsx` rendering the component STANDALONE (it needs no `simulationService` mock because it touches no store but `gameModeStore`), driving clicks with `userEvent`, asserting `aria-pressed` on both buttons, `getState().mode`, `not.toBeDisabled()` on both, and the exact accessible names via `screen.getByRole('button', { name: "Hold'em" })`.

    Append switcher CSS to `src/App.css` under a new phase-tagged banner comment `/* Phase 5 game-mode switcher (05-01) ... */` in the exact multi-line format of the lines 332-343 banner (rationale paragraph + cross-reference to the locked decision numbers). Transcribe the UI-SPEC "Mode Switcher" CSS block exactly: `[data-testid='game-mode-switcher']` gets `display: inline-flex`, `border: 1px solid var(--border)`, `border-radius: 6px`, `overflow: hidden`; `[data-testid^='game-mode-switch-']` gets `min-width: 44px`, `min-height: 44px`, `padding: 8px 16px`, `font-family: inherit`, `font-size: 16px`, `line-height: 1.5`, `font-weight: 400`, `color: var(--text)`, `background: transparent`, `border: none`; `[data-testid='game-mode-switch-holdem']` gets `border-right: 1px solid var(--border)`; and `[data-testid^='game-mode-switch-'][aria-pressed='true']` gets `background: var(--border)`, `color: var(--text-h)`, `font-weight: 600`. Use `data-testid`-scoped selectors, not a bespoke class hierarchy, matching the file's established selector convention. Introduce ZERO new accent usage — the accent budget stays at exactly its three existing reserved uses (UI-SPEC A2); do not add any selector referencing `var(--accent)` in this task.

    Commit with `feat(05-01): add gameModeStore and the GameModeSwitcher control (D-01/D-02)`.
  </action>
  <acceptance_criteria>
    - Source: `src/state/gameModeStore.ts` contains `'holdem' | 'blackjack'`, contains `useGameModeStore`, and contains NONE of the substrings `deckCount`, `gameStore`, `oddsStore`, `pickerStore`, `uiStore`.
    - Source: `src/ui/GameModeSwitcher.tsx` contains the literals `game-mode-switcher`, `game-mode-switch-holdem`, `game-mode-switch-blackjack`, `role="group"`, `aria-label="Game mode"`, `Hold'em`, `Blackjack`, and contains no `disabled` attribute.
    - Source: `src/App.css` contains `[data-testid^='game-mode-switch-'][aria-pressed='true']`, contains `Phase 5 game-mode switcher (05-01)`, and the switcher rules reference `var(--border)`/`var(--text-h)` but never `var(--accent)`.
    - Behavior: `useGameModeStore.getState().mode` is `'holdem'` before any `setMode` call.
    - Test command: `npx vitest run src/state/gameModeStore.test.ts src/ui/GameModeSwitcher.test.tsx` exits 0.
    - Test command: `npm run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/state/gameModeStore.test.ts src/ui/GameModeSwitcher.test.tsx &amp;&amp; npm run lint</automated>
  </verify>
  <done>The store and the switcher exist, are unit-tested green, lint clean, and introduce no accent usage and no deckCount field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN part B — BlackjackScene placeholder and the App.tsx mode fork</name>
  <files>src/ui/BlackjackScene.tsx, src/App.tsx, src/App.css</files>
  <read_first>
    - src/App.tsx (the whole file — the odds effect at lines 37-97 and the JSX body at lines 99-153 are exactly what this task forks)
    - src/ui/TableScene.tsx (the `.felt` wrapper shape the placeholder reuses)
    - src/App.css lines 131-137 (`.empty-hand-state` type-scale rules) and lines 344-355 (the `.felt` rule reused unmodified)
    - .planning/phases/05-game-mode-shell-store-separation/05-UI-SPEC.md sections "Blackjack Placeholder Shell", "Layout & Component Inventory", "Copywriting Contract"
    - .planning/phases/05-game-mode-shell-store-separation/05-PATTERNS.md section "src/App.tsx (modified ...)" — the four-step change shape and its explicit prohibitions
  </read_first>
  <behavior>
    - The full end-to-end test from Task 1 passes: switching to Blackjack mounts the placeholder with the exact locked copy, unmounts every Hold'em element, and switching back restores Hold'em.
    - While mode is `'blackjack'`, `startSimulation` is never called, even when `gameStore.runout` is non-null and `pendingAnimationCount` is 0.
    - Switching away from Hold'em while a run is in flight invokes `cancelSimulation` exactly once, via the effect's EXISTING cleanup — not a new call site.
    - The whole 281-test baseline still passes with no edits to `App.test.tsx` / `App.acceptance.test.tsx` / `App.phase3.acceptance.test.tsx` (default mode is `'holdem'`, so every existing flow is untouched).
  </behavior>
  <action>
    Create `src/ui/BlackjackScene.tsx`. Root element: `<div data-testid="blackjack-scene" className="felt">` — reuse the existing `.felt` class unmodified (D-03: the felt shell IS the placeholder's shell). Inside, render exactly one block: `<div data-testid="blackjack-empty-state">` containing an `<h2>` with the literal `The Blackjack table deals next` and a `<p>` with the literal `Player hand, dealer upcard, live bust and outcome odds, and Stand-vs-Hit choices land here next. Switch back to Hold'em to keep watching odds converge now.` (both locked verbatim by UI-SPEC A3 / Copywriting Contract). Render NO seats, NO community slots, NO deck-origin stack, NO absolute positioning, and — critically — NO `<button>` and no `disabled` control of any kind (D-03 prohibits dead/disabled gameplay controls; this differs deliberately from `CardPicker.tsx`'s disabled-button pattern). Do NOT import or read `gameStore`, `oddsStore`, `pickerStore`, or `uiStore` from this component (D-05). No `aria-live` and no `role="alert"` — this is a static placeholder, matching the `empty-hand-state` precedent.

    Modify `src/App.tsx` in exactly four moves, following the file's own established idioms rather than inventing new ones:

    1. Add `const mode = useGameModeStore((state) => state.mode);` alongside the existing subscribed-value block near `pendingAnimationCount`. It must be a SUBSCRIBED selector value, never a live `getState()` read inside the effect body — the comment at lines 25-28 explains exactly why mixing the two makes an effect that never re-runs.
    2. Add `if (mode !== 'holdem') return;` as the FIRST line inside the effect body, above the existing `pendingAnimationCount > 0` guard, with a D-05-tagged comment explaining that no simulation may start and no odds cache key may be written while another game is on screen (Pitfall 11).
    3. Add `mode` to the effect dependency array, making it `[runout, street, revealedMask, dealNonce, pendingAnimationCount, mode]`. Comment that this is what delivers D-07's cancellation for free: when `mode` flips away mid-run, React tears down the previous effect instance and fires the EXISTING cleanup at lines 93-96 (`ignore = true; void cancelSimulation();`). Do NOT add a second `cancelSimulation()` call inside `setMode`, inside a click handler, or anywhere else — two competing cancellation call sites for the same generation contradicts `simulationService.ts`'s documented one-owner discipline.
    4. Fork the JSX body per UI-SPEC's Layout & Component Inventory: `<h1>` renders unconditionally in both modes (A6); the `.control-bar` wrapper `<div>` also renders unconditionally (A7) with `<GameModeSwitcher />` as its first, unconditional child; `DealButton`, the `set-up-scenario-button`, and `StreetControls` become children rendered only under `mode === 'holdem'`; the `empty-hand-state` block, the `simulation-error` banner, the `card-picker` disclosure wrapper, and the `.table-row` block each become conditional on `mode === 'holdem'` (A8 — `empty-hand-state` and `simulation-error` are gated too, unconditionally on why they would otherwise render); and `<BlackjackScene />` renders under `mode === 'blackjack'`. Use conditional rendering (`&&`), never `display: none` or `hidden` — D-04 and the UI-SPEC Accessibility Contract require the Hold'em elements to be genuinely unmounted, since a merely-hidden focusable button is still a keyboard trap.

    Do NOT call `useUiStore.getState().resetAnimations()` anywhere — it is a TEST-ONLY helper (documented at `uiStore.ts` lines 17-29) and calling it from production would be actively wrong here, because unmounting cards' cleanups run in the same commit and would decrement units armed after the reset. The gate drains for D-07/D-08 purely because move 4 unmounts every `AnimatedCard`/`FlipCard`, firing `useAnimationGate`/`useExitGate`'s existing unmount cleanups. Write no new gate-reset code.

    Append the placeholder CSS to `src/App.css` inside the Phase 5 banner block from Task 2: `[data-testid='blackjack-scene']` gets `display: flex`, `align-items: center`, `justify-content: center`, plus `margin-block: 48px` (the `2xl` token, matching how `.table-row` at line 591 separates the scene block from the control bar); `[data-testid='blackjack-empty-state']` gets `max-width: 420px`, `padding: 24px`, `text-align: center`, `color: var(--seat-badge-text)`. Add no new `z-index` token — the placeholder has no overlapping or animated elements.

    Commit with `feat(05-01): fork App on game mode and add the Blackjack placeholder scene (D-03/D-04/D-05/D-07)`.
  </action>
  <acceptance_criteria>
    - Source: `src/ui/BlackjackScene.tsx` contains `data-testid="blackjack-scene"`, `className="felt"`, `data-testid="blackjack-empty-state"`, and both locked copy strings verbatim; and contains NONE of the substrings `<button`, `disabled`, `gameStore`, `oddsStore`, `pickerStore`, `uiStore`, `aria-live`.
    - Source: `src/App.tsx` contains `useGameModeStore((state) => state.mode)`, contains `if (mode !== 'holdem') return;`, contains the dependency array ending `pendingAnimationCount, mode]`, contains `mode === 'blackjack'`, and contains no occurrence of `resetAnimations` and no occurrence of `deckCount`.
    - Source: `src/App.tsx` contains exactly ONE occurrence of the call form `cancelSimulation(` (the effect cleanup); the import line uses the bare identifier and therefore does not match.
    - Source: `src/App.css` contains `[data-testid='blackjack-empty-state']` with `color: var(--seat-badge-text)`.
    - Source: `src/state/gameStore.ts`, `src/state/oddsStore.ts`, `src/state/pickerStore.ts`, `src/state/uiStore.ts` are byte-unchanged (D-05 zero store-shape changes) — confirm via `git diff --stat`.
    - Behavior: `src/App.modeSwitch.test.tsx` passes in full, including the zero-`<button>`-in-`blackjack-scene` assertion.
    - Test command: `npx vitest run` reports 29+ test files and >= 281 passing tests with 0 failures.
    - Test command: `npm run lint` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Switching modes works end-to-end in both directions, the Hold'em subtree is genuinely unmounted in Blackjack mode, the odds effect is mode-scoped, and the full 281-test baseline plus the new tests are green with no edits to the v1 acceptance suites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → React event handler → `gameModeStore` | The only new input path this phase. Input is a bounded two-value choice originating from two hard-coded `onClick` closures — no free-form user data crosses it. |
| main thread → Web Worker (`simulationService`) | Pre-existing boundary. This phase adds a new *trigger* for teardown across it (a mode change), but no new message shape and no new data. |
| Hold'em `runout` (predetermined future cards) → rendered DOM | Pre-existing Phase 2 D-02 leak-guard boundary. This phase adds a second screen that must never sit on the wrong side of it. |

No network, storage, filesystem, or authentication boundary exists in this client-only app, and this phase adds none. Zero new runtime dependencies.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `gameModeStore.mode` | mitigate | `mode` is a TypeScript literal union `'holdem' \| 'blackjack'` and both writers are hard-coded `setMode('holdem')` / `setMode('blackjack')` closures — no user-supplied string, no URL/query parsing (D-01 explicitly rules out routing), so no untrusted value can reach the field. App.tsx branches on `mode === 'holdem'` / `mode === 'blackjack'` explicitly rather than on a negation, so an impossible third value renders neither game tree rather than falling through into Hold'em. |
| T-05-02 | Information Disclosure | `BlackjackScene` + the Blackjack-mode DOM | mitigate | Task 3 forbids `BlackjackScene` from importing `gameStore`/`oddsStore`/`pickerStore`/`uiStore`, so the predetermined Hold'em `runout` (Phase 2's D-02 leak guard) cannot reach the Blackjack screen. D-04's unmount-not-hide rule additionally removes every Hold'em card element from the DOM in Blackjack mode, so no hidden face asset is inspectable. Enforced by the source assertions in Task 3's acceptance criteria and swept again by Plan 02's DOM-absence test and Plan 03's guard test. |
| T-05-03 | Denial of Service | worker runs + `uiStore.pendingAnimationCount` | mitigate | Rapid mode toggling could (a) strand worker trial loops burning CPU or (b) strand the animation gate above 0, permanently freezing every future odds update. Mitigated by routing the mode change through the effect's existing ignore-flag cleanup (`cancelSimulation`) rather than a second call site, and by draining the gate solely through the existing `useAnimationGate`/`useExitGate` unmount cleanups. Both are proven, not assumed, in Plan 02 Task 2. |
| T-05-04 | Tampering | `oddsStore.settledCache` | mitigate | PITFALLS Pitfall 11: a shared, poker-shaped cache poisoned by a second game's differently-shaped snapshot would render nonsense with no runtime error. Mitigated structurally — the `mode !== 'holdem'` early return means Blackjack mode writes no cache key at all, and Phase 6 gets its own store rather than a discriminated shared one. Verified in Plan 02 Task 1. |
| T-05-05 | Repudiation / Elevation of Privilege | whole app | accept | Client-only static SPA with no accounts, no server, no persisted state, and no privilege levels. There is nothing to repudiate and no privilege to elevate. |
| T-05-SC | Tampering | npm installs | accept | Zero package-manager installs this phase — v2.0's locked constraint is zero new runtime dependencies, and this plan adds only hand-written TypeScript on the existing stack. No `[ASSUMED]`/`[SUS]` package exists to gate, so no legitimacy checkpoint is required. Any executor that finds itself running `npm install <package>` must stop and escalate instead. |
</threat_model>

<verification>
1. `npx vitest run` — 29+ files, >= 281 tests, 0 failures.
2. `npm run lint` — exits 0 (no new ESLint suppressions introduced).
3. `npm run build` — exits 0 (`tsc -b` type-checks the new literal union and the forked JSX tree).
4. `git diff --stat` shows NO changes to `src/state/gameStore.ts`, `src/state/oddsStore.ts`, `src/state/pickerStore.ts`, `src/state/uiStore.ts`, `src/App.test.tsx`, `src/App.acceptance.test.tsx`, `src/App.phase3.acceptance.test.tsx`.
5. `grep -c 'cancelSimulation(' src/App.tsx` returns 1.
</verification>

<success_criteria>
- A segmented `game-mode-switcher` with exactly the labels `Hold'em` and `Blackjack` renders in the control bar in BOTH modes, with `aria-pressed` reflecting the active game and neither button ever disabled (D-01).
- `src/state/gameModeStore.ts` exists holding exactly `{ mode, setMode }` with no `deckCount` and no cross-store import (D-02, D-10).
- Blackjack mode renders a felt-shell `blackjack-scene` containing only the locked `blackjack-empty-state` copy and zero interactive controls (D-03).
- Zero Hold'em testids and no Deal button exist in the DOM while in Blackjack mode (D-04).
- The odds effect returns early on non-Hold'em mode and lists `mode` in its dependency array; the four Hold'em stores are byte-unchanged (D-05).
- Cancellation and gate drain ride existing mechanisms — exactly one `cancelSimulation(` call site, zero production `resetAnimations` calls (D-07, D-08).
- Full baseline suite green with no edits to the v1 acceptance suites (D-09).
</success_criteria>

<output>
Create `.planning/phases/05-game-mode-shell-store-separation/05-01-SUMMARY.md` when done.

Record in the SUMMARY: the final `npx vitest run` file/test counts against the 29/281 baseline, and an explicit statement that no v1 acceptance suite was edited (or, if one was, the exact mechanical adjustment and its D-09 justification).
</output>
