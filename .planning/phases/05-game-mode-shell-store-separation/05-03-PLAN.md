---
phase: 05-game-mode-shell-store-separation
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/App.modeShell.guard.test.ts
autonomous: false
requirements: [BJ-01]

must_haves:
  truths:
    - "A source-shape guard proves gameModeStore holds no deckCount field and imports no other store, so it stays the only cross-game store (D-02, D-05)"
    - "The guard proves the four Hold'em stores and engine/conditioning.ts contain no blackjack/gameMode branch — PITFALLS Pitfall 10's mode-leakage warning sign cannot reappear silently (D-05)"
    - "The guard proves App.tsx has exactly one cancelSimulation( call site and zero production resetAnimations calls, so cancellation and gate-drain stay on the existing mechanisms (D-07, D-08)"
    - "The guard proves oddsStore's knowledgeKey is still the exact poker-shaped two-part key with no game discriminant bolted on (Pitfall 11)"
    - "The guard proves no Phase 5 file mentions deckCount at all, so the Phase 4 traps stay untripped (D-10, WR-02, WR-03)"
    - "The guard pins the UI-SPEC's locked copy and label strings so a future reword goes red (D-01, D-03)"
    - "The full suite passes at 281 baseline tests plus this phase's additions, with every App-level adjustment enumerated and justified in the SUMMARY (D-09)"
    - "A human confirms in a real browser that the switch works both ways, the placeholder is honest, and Hold'em's full loop is unharmed (BJ-01 acceptance)"
  artifacts:
    - path: "src/App.modeShell.guard.test.ts"
      provides: "Cross-cutting source-shape guards for the mode shell's negative invariants"
      contains: "@vitest-environment node"
  key_links:
    - from: "src/App.modeShell.guard.test.ts"
      to: "src/state/gameModeStore.ts, src/App.tsx, src/ui/BlackjackScene.tsx, src/state/*.ts"
      via: "readFileSync source-text assertions, following the shoePath.guard.test.ts precedent"
      pattern: "readFileSync"
---

## Phase Goal

**As a** probability explorer using the simulator, **I want to** switch between Hold'em and Blackjack via an on-screen mode switcher, **so that** each game keeps fully independent state and odds and neither leaks into or corrupts the other.

<objective>
Close the phase: pin the negative invariants that behavioral tests cannot express, run the full regression bar, and get human acceptance in a real browser.

Purpose: D-09's "281 tests pass with at most justified mechanical adjustments" is the phase's regression contract and needs an explicit, documented gate. The negative invariants (no second cancellation call site, no production gate reset, no deckCount, no mode branch inside Hold'em stores) are the constraints Phases 6-8 will be tempted to violate first — they need a red test guarding them, not a paragraph in a SUMMARY.
Output: one new guard test file, a documented full-suite result, and a human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-game-mode-shell-store-separation/05-CONTEXT.md
@.planning/phases/05-game-mode-shell-store-separation/05-UI-SPEC.md
@.planning/phases/05-game-mode-shell-store-separation/05-01-SUMMARY.md
@.planning/phases/05-game-mode-shell-store-separation/05-02-SUMMARY.md

@src/engine/shoePath.guard.test.ts

<interfaces>
<!-- The guard file reads source text, not exports. These are the exact strings it pins. -->

Locked by `05-UI-SPEC.md` Copywriting Contract (must appear verbatim in `src/ui/BlackjackScene.tsx`):
- `The Blackjack table deals next`
- `Player hand, dealer upcard, live bust and outcome odds, and Stand-vs-Hit choices land here next. Switch back to Hold'em to keep watching odds converge now.`

Locked by D-01 (must appear verbatim in `src/ui/GameModeSwitcher.tsx`):
- `Hold'em`
- `Blackjack`

Locked testids (UI-SPEC "Testids — NEW this phase"):
- `game-mode-switcher`, `game-mode-switch-holdem`, `game-mode-switch-blackjack`, `blackjack-scene`, `blackjack-empty-state`

Pinned unchanged in `src/state/oddsStore.ts` (Pitfall 11 — no game discriminant this phase; Phase 6 gets its own store instead):
```typescript
export function knowledgeKey(street: Street, revealedMask: number): string {
  return `${street}|${revealedMask}`;
}
```
</interfaces>

**Baseline:** `npx vitest run` measured **29 test files / 281 tests passing** on 2026-08-24 before any Phase 5 work.

**Comment-hygiene rule for this guard file.** Several assertions here are of the form "file X must NOT contain token T". The guard file itself will name those tokens in its own comments and assertion messages — but it only ever reads OTHER files' source, never its own, so there is no self-invalidation risk. Where an assertion counts occurrences inside a target file, strip comment lines before counting (drop lines whose trimmed form starts with `//` or `*`) so a future explanatory comment mentioning the token cannot flip the count. Never gate on an unfiltered raw-count equality.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mode-shell source-shape guard</name>
  <files>src/App.modeShell.guard.test.ts</files>
  <read_first>
    - src/engine/shoePath.guard.test.ts (the exact file shape to copy: `// @vitest-environment node` pragma, `readSource()` helper, `it.each` over file lists, assertion messages that explain the invariant)
    - src/state/gameModeStore.ts, src/ui/GameModeSwitcher.tsx, src/ui/BlackjackScene.tsx (Plan 01's output — the files being pinned)
    - src/App.tsx (the forked effect and render tree whose call-site counts are pinned)
    - src/state/oddsStore.ts lines 9-12 (the knowledgeKey body being pinned)
  </read_first>
  <behavior>
    - `state/gameModeStore.ts` contains none of: `deckCount`, `gameStore`, `oddsStore`, `pickerStore`, `uiStore` — it is the only cross-game store and it reads nothing else (D-02, D-05, ARCHITECTURE (a)).
    - `state/gameStore.ts`, `state/oddsStore.ts`, `state/pickerStore.ts`, `state/uiStore.ts`, and `engine/conditioning.ts` each contain neither `blackjack` nor `gameMode` (case-insensitive) — PITFALLS Pitfall 10's stated warning sign is "any `if (mode === 'blackjack')` branch appearing inside gameStore.ts, deriveConditionedState, or the existing App.tsx effect"; the first two are covered here, the third by the App.tsx assertions below.
    - `App.tsx`, comment lines stripped, contains exactly ONE occurrence of the call form `cancelSimulation(` (D-07: one cancellation owner, the effect cleanup; the import statement uses the bare identifier and does not match the call form).
    - `App.tsx`, `state/gameModeStore.ts`, `ui/GameModeSwitcher.tsx` and `ui/BlackjackScene.tsx` contain zero occurrences of `resetAnimations` (D-07/D-08: gate drain is unmount-cleanup-only; `uiStore.resetAnimations` is TEST-ONLY).
    - `App.tsx` contains `if (mode !== 'holdem') return;` and a dependency array whose text ends `pendingAnimationCount, mode]` (D-05: the effect is mode-scoped AND mode is a real trigger — one without the other is a silent bug).
    - `App.tsx`, `state/gameModeStore.ts`, `ui/GameModeSwitcher.tsx` and `ui/BlackjackScene.tsx` contain zero occurrences of `deckCount` (D-10, WR-02, WR-03: the deck-count wire path is Phase 6/8's, and nothing here may pass `deckCount: 2` into the Hold'em trial path before Phase 7's duplicate-aware evaluator exists).
    - `ui/BlackjackScene.tsx` contains none of `<button`, `gameStore`, `oddsStore`, `pickerStore`, `uiStore` (D-03, D-05).
    - `ui/BlackjackScene.tsx` contains both locked copy strings verbatim; `ui/GameModeSwitcher.tsx` contains both locked labels and all three locked switcher testids (D-01, D-03).
    - `state/oddsStore.ts` still contains the exact two-part `knowledgeKey` return expression with no game discriminant prefix (Pitfall 11).
  </behavior>
  <action>
    Create `src/App.modeShell.guard.test.ts` (a `.ts` file, not `.tsx` — it renders nothing). Open with the `// @vitest-environment node` pragma on line 1, exactly as `src/engine/shoePath.guard.test.ts` does, and copy that file's `readSource(relativePath)` helper built on `readFileSync` + `fileURLToPath` + `dirname`/`join`, with `SRC_DIR` resolved relative to this file's own location.

    Write a file-level rationale comment naming the D-numbers and pitfall numbers this file protects and stating the standing rule for future phases: when Phase 6 legitimately needs one of these tokens (for example `deckCount` in `gameModeStore.ts`), the correct move is to amend THIS guard in the same commit as the feature, with the phase decision cited — never to delete the assertion. Model the tone on `shoePath.guard.test.ts` lines 7-19.

    Structure the assertions as `describe` blocks titled `'<subject> — <invariant> (D-NN)'`, using `it.each` over explicit file-path arrays wherever the same invariant spans several files, and giving every `expect` a second-argument message that states WHY the invariant exists, not just that it failed — `shoePath.guard.test.ts` lines 35, 39, 50 and 57 are the reference for that message style.

    For the occurrence-count assertion on `cancelSimulation(`, first strip comment lines: split the source on newlines, drop lines whose trimmed form starts with `//` or `*`, rejoin, then count. State in a comment that the strip exists so a future explanatory comment mentioning the token cannot flip the count. Do not use a bare unfiltered count.

    For the case-insensitive `blackjack`/`gameMode` scan of the Hold'em stores, lowercase the source once and search the lowercased text, so `Blackjack`, `BLACKJACK` and `useGameModeStore` are all caught.

    Keep every locked string in this file as a single `const` declared once at module scope and referenced by the assertions, so the verbatim text lives in exactly one place — matching how the codebase states each locked user-facing string once.

    Commit with `test(05-03): pin the mode-shell negative invariants as a source-shape guard (D-02/D-05/D-07/D-08/D-10)`.
  </action>
  <acceptance_criteria>
    - Source: `src/App.modeShell.guard.test.ts` line 1 is exactly `// @vitest-environment node`.
    - Source: the file contains `readFileSync` and an `it.each` over a file-path array.
    - Source: the comment-stripping step exists before the `cancelSimulation(` count, and the count assertion is `toBe(1)`.
    - Behavior: `npx vitest run src/App.modeShell.guard.test.ts` exits 0.
    - Behavior (negative control, run manually and revert): temporarily adding the literal `deckCount` to `src/state/gameModeStore.ts` turns this guard red; temporarily adding a second `cancelSimulation(...)` call to `src/App.tsx` turns it red. Both must be reverted before commit.
    - Source: `git diff --stat` lists only the new guard file — no production file changed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/App.modeShell.guard.test.ts &amp;&amp; npm run lint</automated>
  </verify>
  <done>The mode shell's negative invariants are pinned by a guard that demonstrably goes red when violated, following the established Phase 4 guard-file precedent.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression sweep and D-09 accounting</name>
  <files>(no source changes — this task produces the SUMMARY's regression record)</files>
  <read_first>
    - .planning/phases/05-game-mode-shell-store-separation/05-01-SUMMARY.md and 05-02-SUMMARY.md (the per-plan test-count deltas being reconciled)
    - .planning/phases/05-game-mode-shell-store-separation/05-CONTEXT.md D-09 and D-10 (the regression bar and the Phase 4 traps)
  </read_first>
  <action>
    Run the three gates in order and capture the exact output figures: `npx vitest run` (record test-file count and test count), `npm run lint`, `npm run build`.

    Reconcile the test count against the pinned 29-file / 281-test baseline. The expected shape is 281 pre-existing tests still passing PLUS the cases added by Plans 01-03. If the pre-existing 281 is not intact, do not adjust a v1 test to make it pass — treat it as a real regression in Plan 01's production code and fix the production code.

    Enumerate every adjustment made to any pre-existing test file across the whole phase (ideally zero, since default mode is `holdem`). For each one, record the file, the exact diff, and the D-09 justification for why it is mechanical (for example, a selector that now matches two buttons because the switcher joined the control bar) rather than a weakening of an assertion. A change that relaxes an assertion, deletes a case, or adds `.skip`/`.todo` is NOT mechanical and is not permitted under D-09.

    Verify the Phase 4 traps stayed untripped: `git diff --stat` against the phase's base commit must show no changes under `src/engine/`, `src/worker/`, or `src/state/pickerStore.ts`, and `git grep -n 'deckCount' -- src/App.tsx src/state/gameModeStore.ts src/ui/GameModeSwitcher.tsx src/ui/BlackjackScene.tsx` must return nothing (D-10, WR-02, WR-03).

    Confirm no dev server was left running from any earlier task before handing off to the checkpoint.
  </action>
  <acceptance_criteria>
    - Test command: `npx vitest run` exits 0 with 0 failures, 0 skipped, and a test count >= 281.
    - Test command: `npm run lint` exits 0 with no new inline ESLint disable comments introduced anywhere in the phase.
    - Test command: `npm run build` exits 0.
    - Behavior: `git grep -n 'deckCount' -- src/App.tsx src/state/gameModeStore.ts src/ui/GameModeSwitcher.tsx src/ui/BlackjackScene.tsx` produces no output.
    - Behavior: `git diff --stat <phase-base-commit>..HEAD -- src/engine src/worker src/state/pickerStore.ts` produces no output.
    - Source: the SUMMARY lists every pre-existing-test adjustment with its D-09 justification, or states explicitly that there were none.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; ! git grep -q 'deckCount' -- src/App.tsx src/state/gameModeStore.ts src/ui/GameModeSwitcher.tsx src/ui/BlackjackScene.tsx</automated>
  </verify>
  <done>All three gates are green, the 281-test baseline is intact, every adjustment is enumerated and justified, and the Phase 4 traps are confirmed untripped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser acceptance of the mode shell (BJ-01)</name>
  <files>(no source changes — this is a blocking human verification of the running app)</files>
  <read_first>
    - .planning/phases/05-game-mode-shell-store-separation/05-UI-SPEC.md sections "Mode Switcher", "Blackjack Placeholder Shell", "Copywriting Contract" (the visual and copy contract being accepted)
    - .planning/phases/05-game-mode-shell-store-separation/05-CONTEXT.md D-01, D-03, D-04, D-07, D-08 (the decisions this checkpoint accepts)
  </read_first>
  <action>Present the verification script below to the developer and BLOCK. Do not proceed, do not mark the phase complete, and do not auto-approve — this checkpoint is never auto-advanceable regardless of workflow settings. Start the dev server yourself with `npm run dev` in the background, report the local URL, and stop the server once the developer responds. If the developer reports a failure, record it verbatim in the SUMMARY and route the fix back to the owning plan (Plan 01 for rendering/copy/fork defects, Plan 02 for a missed isolation angle) rather than patching around it here.</action>
  <what-built>
    A `Hold'em` / `Blackjack` segmented switcher in the top control bar, a Blackjack placeholder screen on the felt, and full store/DOM isolation between the two — with the Hold'em subtree genuinely unmounted in Blackjack mode, in-flight simulations cancelled on switch-away, and Hold'em state restored intact on return. Automated coverage: an end-to-end switch test, a store/cache/DOM isolation suite, a switch-mid-deal race test, and a source-shape guard, on top of the intact 281-test v1 baseline.
  </what-built>
  <how-to-verify>
    Start the dev server with `npm run dev` and open the printed local URL (typically http://localhost:5173).

    1. **Switcher present and legible.** Confirm the control bar shows two adjacent buttons reading exactly `Hold'em` and `Blackjack`, edge-to-edge with a shared border and a single rounded outline. `Hold'em` should be the active one: filled background, bolder text. Confirm it uses NO purple accent color — the accent stays reserved for the street label, filled picker slots, and the enabled Advance button.
    2. **Hold'em still works.** Click `Deal`. Watch the cards deal and the odds converge. Click `Advance` through flop, turn and river; click `Rewind` back. Reveal an opponent seat. Open `Set Up Scenario` and pick a card. Everything must behave exactly as it did before this phase.
    3. **Switch to Blackjack.** Click `Blackjack`. The felt should show a single centered white text block: heading `The Blackjack table deals next`, body naming player hand, dealer upcard, bust/outcome odds and Stand-vs-Hit. Confirm there is NO Deal button, NO street controls, NO odds panel, NO seats, and — importantly — no greyed-out or disabled gameplay controls anywhere. `Blackjack` should now be the filled/bold segment.
    4. **Switch back.** Click `Hold'em`. The exact table you left should be back: same hole cards, same board, same street, same revealed opponents, and the same settled odds numbers. Nothing should re-deal and the trial counter should not restart from zero.
    5. **Switch mid-deal (the race).** Click `Deal`, and while the cards are still visibly flying, immediately click `Blackjack`. Then click `Hold'em` again. The odds panel must come back to life and show numbers — if it stays dimmed with em dashes forever, the animation gate stranded and this step FAILS.
    6. **Rapid toggling.** Click back and forth between the two buttons a dozen times quickly. Then deal in Hold'em. Odds must still converge normally, and the browser console must show no errors.
    7. **Keyboard and focus.** Tab to the switcher, press Enter or Space to switch. Confirm a visible focus ring, and that neither button is ever greyed out — clicking the already-active game should be a harmless no-op.
    8. **Console clean.** Check DevTools for errors or React warnings across all of the above.

    Stop the dev server when finished.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5, or describe exactly which step failed and what you saw.</resume-signal>
  <acceptance_criteria>
    - Behavior: the developer confirms steps 1-8 pass, or names the exact failing step.
    - Behavior: step 5 (switch mid-deal) leaves the odds panel live — a permanently dimmed panel with em dashes is a FAIL, not a cosmetic note.
    - Behavior: step 3 shows zero interactive controls inside the Blackjack felt — a greyed-out or disabled control is a FAIL under D-03.
    - Behavior: the browser console shows no errors or React warnings across all steps.
    - Source: the developer verdict and any observations are recorded verbatim in 05-03-SUMMARY.md.
    - Behavior: no dev server process remains running after the checkpoint resolves.
  </acceptance_criteria>
  <verify>
    <human-check>Developer confirms in a real browser: switcher renders with the locked labels and a non-accent active treatment; Hold’em’s full loop (deal, street nav, rewind, reveal, picker) is unharmed; the Blackjack placeholder shows the locked copy with zero controls; switching back restores the exact table and settled odds; a mid-deal switch does not strand the odds panel; rapid toggling leaves the console clean.</human-check>
    <automated>npx vitest run &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>The developer has typed "approved" (or all reported failures have been fixed in their owning plan and re-verified), the verdict is recorded in the SUMMARY, and no dev server is left running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main thread → Web Worker | The browser checkpoint is the ONLY place in this phase where the real worker boundary is exercised — every automated test mocks `simulationService`. Steps 5 and 6 are the real-worker cancellation and rapid-toggle checks. |
| user click → `gameModeStore` | Exercised for real, including keyboard activation, in checkpoint step 7. |
| source text → guard assertions | Task 1 reads other files' source only, never its own, so the guard cannot self-invalidate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | worker runs + animation gate, against the REAL worker | mitigate | Checkpoint steps 5 and 6 exercise mid-deal switching and rapid toggling against the real Comlink worker and real Motion animations — the one path the mocked jsdom suites structurally cannot cover. A stranded gate (permanently dimmed odds panel) or an orphaned trial loop (console errors, dead odds) fails the checkpoint. |
| T-05-07 | Tampering | future-phase erosion of the shell's invariants | mitigate | Task 1's guard makes the negative invariants (one cancellation owner, no production gate reset, no `deckCount`, no mode branch in Hold'em stores, no game discriminant on `knowledgeKey`) enforceable rather than advisory, with an explicit written rule that Phase 6 must amend the guard alongside the feature rather than delete the assertion. |
| T-05-02 | Information Disclosure | Hold'em card data on the Blackjack screen | mitigate | Guard assertions prove `BlackjackScene.tsx` imports no Hold'em store; checkpoint step 3 confirms visually that no Hold'em element renders. |
| T-05-08 | Tampering | regression-bar erosion | mitigate | Task 2 forbids relaxed assertions, deleted cases and `.skip`/`.todo` as ways of satisfying D-09, and requires every adjustment to a pre-existing test to be enumerated with its justification in the SUMMARY. |
| T-05-05 | Spoofing / Repudiation / Elevation of Privilege | whole app | accept | Client-only static SPA, no accounts, no server, no persisted state, no privilege levels. Nothing to spoof, repudiate or elevate. |
| T-05-SC | Tampering | npm installs | accept | Zero package-manager installs this phase. `npm ci` from the committed lockfile only — no new or updated dependency is introduced, so no `[ASSUMED]`/`[SUS]` package legitimacy checkpoint applies. An executor reaching for `npm install <package>` must stop and escalate. |
</threat_model>

<verification>
1. `npx vitest run` — 0 failures, 0 skipped, >= 281 tests.
2. `npm run lint` — exits 0.
3. `npm run build` — exits 0.
4. Guard negative controls demonstrated red, then reverted (recorded in the SUMMARY).
5. Human checkpoint returns "approved".
6. No dev server left running.
</verification>

<success_criteria>
- The mode shell's negative invariants are pinned by a guard test that demonstrably fails when violated (D-02, D-05, D-07, D-08, D-10).
- The 281-test v1 baseline is intact, with every adjustment to a pre-existing test enumerated and justified as mechanical (D-09).
- The Phase 4 traps are confirmed untripped: no `src/engine/`, `src/worker/` or `pickerStore.ts` changes, and no `deckCount` anywhere in the Phase 5 files (D-10, WR-02, WR-03).
- A human has confirmed in a real browser that switching works both ways, the placeholder is honest and control-free, Hold'em's full loop is unharmed, and the mid-deal switch does not strand the odds panel — closing BJ-01.
</success_criteria>

<output>
Create `.planning/phases/05-game-mode-shell-store-separation/05-03-SUMMARY.md` when done.

Record in the SUMMARY: the final test-file/test counts, the guard negative-control results, the full enumeration of pre-existing-test adjustments (or an explicit "none"), and the human checkpoint verdict with any noted observations.
</output>
