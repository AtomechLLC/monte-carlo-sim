---
phase: 02-scenario-construction-street-navigation
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - src/App.tsx
  - src/App.css
  - src/index.css
  - src/ui/CardPicker.tsx
  - src/ui/StreetControls.tsx
  - src/App.acceptance.test.tsx
autonomous: true
requirements: [DEAL-02, DEAL-03, NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "A first-time visitor sees an explicit prompt telling them how to start, not a blank screen"
    - "Every new control is keyboard reachable, has a visible focus ring, and announces its disabled state natively"
    - "Every interactive control introduced this phase has at least a 44x44px hit area"
    - "The full loop — pick cards, deal, advance, rewind, reveal — is covered by one end-to-end test asserting the exact conditioning at each step"
  artifacts:
    - path: "src/App.acceptance.test.tsx"
      provides: "End-to-end coverage of the four ROADMAP Phase 2 success criteria"
      min_lines: 90
    - path: "src/App.css"
      provides: "Phase 2 conformance styling — spacing scale, accent roles, 44px hit areas, disabled dimming"
      contains: "44px"
  key_links:
    - from: "src/App.tsx"
      to: "src/App.css"
      via: "empty-hand-state and simulation-error class hooks"
      pattern: "empty-hand-state"
    - from: "src/App.acceptance.test.tsx"
      to: "src/state/simulationService.ts"
      via: "explicit vi.mock factory capturing conditioned states"
      pattern: "vi.mock"
---

## Phase Goal

**As a** first-time visitor, **I want to** land on a screen that tells me what to do and responds legibly to keyboard and screen reader, **so that** the whole scenario-construction loop is usable, not just functional.

<objective>
Bring the Phase 2 UI up to the approved design contract and prove the whole interaction loop end to end in one test file.

Purpose: The individual capability plans deliberately deferred styling and the pre-deal empty state to avoid CSS conflicts across four plans. This is the single conformance pass, plus the acceptance suite that checks all four ROADMAP success criteria in one place rather than scattered across unit tests.

Output: The `empty-hand-state` block, one CSS pass over `App.css`/`index.css` implementing the UI-SPEC spacing/colour/hit-area rules, and `src/App.acceptance.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scenario-construction-street-navigation/02-UI-SPEC.md
@.planning/phases/02-scenario-construction-street-navigation/02-CONTEXT.md
@.planning/phases/02-scenario-construction-street-navigation/02-04-SUMMARY.md

<interfaces>
Existing CSS custom properties in `src/index.css` (light values shown; a `prefers-color-scheme: dark` block already overrides each one):
```css
--text: #6b6375;   --text-h: #08060d;   --bg: #fff;
--border: #e5e4e7; --accent: #aa3bff;   --accent-bg: rgba(170, 59, 255, 0.1);
--accent-border: rgba(170, 59, 255, 0.5);
--sans: system-ui, 'Segoe UI', Roboto, sans-serif;
```
Dark-mode counterparts already defined: `--bg: #16171d`, `--border: #2e303a`, `--accent: #c084fc`.

Phase 2 testid contract (all must exist by the end of this plan):
`hero-hole`, `opponents`, `trial-counter`, `win-pct`, `tie-pct`, `lose-pct`, `category-table`, `category-pct-0`..`category-pct-9`,
`empty-hand-state`, `simulation-error`, `card-picker`, `picker-slot-hero-0`, `picker-slot-hero-1`, `picker-slot-flop-0`,
`picker-slot-flop-1`, `picker-slot-flop-2`, `picker-slot-turn`, `picker-slot-river`, `picker-clear-{slotId}`,
`picker-clear-all`, `picker-panel`, `picker-card-{cardCode}`, `rewind-button`, `advance-button`, `street-label`,
`board-cards`, `board-empty-state`, `opponent-seat-0`, `opponent-seat-1`, `opponent-seat-2`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Empty-hand state and the single UI-SPEC conformance pass</name>
  <files>src/App.tsx, src/App.css, src/index.css, src/ui/CardPicker.tsx, src/ui/StreetControls.tsx</files>
  <read_first>
    - .planning/phases/02-scenario-construction-street-navigation/02-UI-SPEC.md sections "Spacing Scale", "Typography", "Color", "Layout & Component Inventory", "Interaction States & Testid Contract", "Copywriting Contract", "Accessibility Contract"
    - src/index.css (existing custom properties, the light/dark `prefers-color-scheme` structure, and the base `font: 18px/145% var(--sans)` rule)
    - src/App.css (existing `.counter:focus-visible` pattern to follow, and the Phase 1 scaffold selectors that must not be broken)
    - src/App.tsx (current JSX order and the `simulation-error` banner added in 02-02)
    - src/ui/CardPicker.tsx (elements needing class hooks for slot/filled/used styling)
    - src/ui/StreetControls.tsx (elements needing class hooks for the accent street label and accent Advance label)
  </read_first>
  <action>
    Add the pre-deal empty state to `src/App.tsx`, positioned directly after the `<h1>` and before the `simulation-error` banner per the UI-SPEC document order. Render it only while `runout` is `null`. It carries `data-testid="empty-hand-state"`, a heading with the exact text `No hand dealt yet`, and body text exactly `Click Deal to draw a random hand, or use the card picker below to set up your own scenario, then click Deal.`

    Add a `--destructive` custom property to `src/index.css` in both the light block (`#b91c1c`) and the `prefers-color-scheme: dark` block (`#f87171`); it does not exist yet. Do not add any other new colour tokens — the contract reuses `--bg`, `--border`, `--accent`, `--text`.

    Make one styling pass in `src/App.css` covering the whole phase's new UI. Implement, in terms of the existing custom properties:
    - Vertical rhythm: 16px between the top-level page sections (card picker block, street block, hand block, board block, odds block); 8px between a slot button and its Clear sibling and between Rewind / street label / Advance; 4px between rank buttons inside a suit group; 24px padding inside the `picker-panel` dialog.
    - Accent (`var(--accent)`) used for exactly three things and nothing else: the current street-name label text, the text of a filled picker slot button, and the Advance button's label while it is enabled. Never as a background fill and never on the Deal button.
    - `--destructive` used for exactly one thing: the `simulation-error` banner's text and left border.
    - Disabled controls (used picker cards, Rewind at pre-flop, Advance at river, empty Clear buttons, revealed opponent seats): `color: var(--text)` at `opacity: 0.4` with `cursor: not-allowed`. Do not use the destructive colour for these — they are neutral, not destructive.
    - A minimum 44x44px hit area on every interactive element introduced this phase, achieved with padding rather than by enlarging the visible text.
    - Borders on picker slot buttons, the panel container, opponent seat buttons and board card cells use `var(--border)`.
    - Two font weights only for new elements: 400 for body/label text, 600 for the three new `<h2>` headings.
    Do not add any rule that removes or overrides `:focus-visible` outlines, and do not touch the existing `<h1>` styling (Phase 3 owns that redesign).

    Add only the minimal `className` hooks these rules need to `src/ui/CardPicker.tsx` and `src/ui/StreetControls.tsx`. Do not change any text, testid, `disabled` condition or DOM structure in those files — this task is styling plus the empty state, nothing else.
  </action>
  <verify>
    <automated>npm test && npx eslint . && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx eslint .`, `npx tsc -b` and `npm run build` all exit 0.
    - `grep -c 'No hand dealt yet' src/App.tsx` returns 1 and `grep -c 'data-testid="empty-hand-state"' src/App.tsx` returns 1.
    - `grep -c -- '--destructive' src/index.css` returns 2 or more (light plus dark block).
    - `grep -c '44px' src/App.css` returns 1 or more.
    - `grep -v '^\s*/\*' src/App.css | grep -c 'outline: *none\|outline: *0'` returns 0.
    - `git diff --stat src/ui/CardPicker.tsx src/ui/StreetControls.tsx` shows changes only on lines containing `className` (verify by inspecting `git diff` for those two files).
    - `grep -c 'var(--accent)' src/App.css` returns between 1 and 4 inclusive (accent is reserved for three roles).
  </acceptance_criteria>
  <done>The app opens with a clear call to action, every new control meets the 44px hit-area floor with native disabled semantics and intact focus rings, and the accent and destructive colours are each used only for their contracted roles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end acceptance suite for the four Phase 2 success criteria</name>
  <files>src/App.acceptance.test.tsx</files>
  <read_first>
    - .planning/ROADMAP.md section "Phase 2" (the four Success Criteria this suite must map to one-for-one)
    - src/App.test.tsx (the explicit `vi.mock` factory for `simulationService`, `beforeEach` store resets, and the streamed-snapshot `mockImplementation` template)
    - src/App.tsx (final JSX and effect after Task 1)
    - src/state/gameStore.ts, src/state/pickerStore.ts, src/state/oddsStore.ts (final store contracts)
    - src/engine/conditioning.ts (deriveConditionedState, for asserting expected partitions)
  </read_first>
  <behavior>
    - Criterion 1 (DEAL-02/DEAL-03): pick `As` into `hero-0` and `Kd` into `flop-0`, deal, and assert `runout.heroHole[0] === 'As'`, `runout.board[0] === 'Kd'`, all 13 runout cards distinct, and that reopening the panel from another slot shows both `picker-card-As` and `picker-card-Kd` disabled with the `(used)` suffix.
    - Criterion 2 (NAV-01): deal, then advance three times, asserting the conditioned state handed to `startSimulation` has `knownBoard.length` of 0, 3, 4 then 5 in order, and that `board-cards` shows 3, 4 then 5 children.
    - Criterion 3 (NAV-02): settle the flop with a `done` snapshot, rewind to pre-flop, advance back to the flop, assert no additional `startSimulation` call for that revisit and that `win-pct` matches the settled flop value; separately assert `runout` is object-identical across the rewind and re-advance (same cards).
    - Criterion 4 (NAV-03): click `opponent-seat-0`, assert the seat becomes `disabled` and shows the two card codes, assert the next conditioned state has `knownOpponentHoles[0]` non-null, then rewind to pre-flop and assert a fresh conditioned state arrives with `knownBoard.length === 0` and `knownOpponentHoles[0]` non-null.
    - D-02 guard: in every captured conditioned state, `remainingDeck` contains every board card not yet visible at that street and every hole card of every still-hidden opponent.
    - The pre-deal screen shows `empty-hand-state`, and it disappears after the first deal.
  </behavior>
  <action>
    Create `src/App.acceptance.test.tsx` as a standalone suite (do not grow `App.test.tsx`, which stays focused on wiring-level regressions). Mock `./state/simulationService` with the same explicit factory used in `App.test.tsx` — including the comment explaining why automocking instantiates a real Worker under jsdom — exporting both `startSimulation` and `cancelSimulation` as `vi.fn()`.

    Give the mock a `mockImplementation` that records each conditioned state into a module-level array and immediately streams one settled snapshot (`done: true`) with recognisably distinct `outcomes` per call, so cache-hit assertions can distinguish a served cached value from a fresh run.

    Structure the file as four `describe` blocks named after the four ROADMAP success criteria verbatim, plus a fifth for the D-02 guard and the empty state. Reset `useGameStore`, `usePickerStore` and `useOddsStore` (including `clearCache()`) in `beforeEach`. Drive every interaction through the DOM with `userEvent` — never by calling store actions directly — so the suite proves the wiring, not just the stores.
  </action>
  <verify>
    <automated>npx vitest run src/App.acceptance.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/App.acceptance.test.tsx` exits 0 with at least four `describe` blocks whose names correspond to the four ROADMAP Phase 2 success criteria.
    - A test asserts `knownBoard.length` progresses 0 → 3 → 4 → 5 across successive `startSimulation` calls.
    - A test asserts the flop revisit after a rewind adds no new `startSimulation` call and that `win-pct` equals the value settled on the first flop visit.
    - A test asserts, for the pre-flop conditioned state, that `remainingDeck` contains all five board cards and all six opponent hole cards.
    - A test asserts `empty-hand-state` is present before the first deal and absent afterwards.
    - `grep -c 'store action' src/App.acceptance.test.tsx` is irrelevant; instead confirm `grep -c 'getState().deal()\|getState().advanceStreet()\|getState().reveal(' src/App.acceptance.test.tsx` returns 0 — all interaction goes through `userEvent`.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>One suite demonstrates all four ROADMAP Phase 2 success criteria through real user interactions, and pins the D-02 no-peeking invariant at the integration level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stylesheet → rendered UI | CSS can visually hide an interactive control's state without changing its semantics. |
| Test suite → production code | Acceptance assertions are the last automated gate before human verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Information disclosure | CSS-only disabled styling that leaves a control operable | mitigate | Every disabled state uses the native `disabled` attribute (asserted in plans 02-03/02-04 tests); this plan's CSS only dims, and an acceptance grep confirms no `outline: none` reset hides focus. |
| T-02-19 | Tampering | Odds display contradicting the visible knowledge state | mitigate | Task 2's D-02 guard asserts hidden cards remain in `remainingDeck` for every captured conditioned state. |
| T-02-20 | Denial of service | Focus trap or Escape handling broken by styling | accept | No JS behaviour is changed in this plan; the native `<dialog>` semantics from 02-04 are untouched and covered by that plan's tests. |
| T-02-SC | Tampering | npm installs | accept | No packages added — the contract explicitly declines a design system this phase (UI-SPEC A1). If a styling library seems necessary, stop and escalate. |
</threat_model>

<verification>
- `npm test`, `npx eslint .`, `npx tsc -b` and `npm run build` all exit 0.
- Every testid in the `<interfaces>` contract list resolves in at least one passing test.
</verification>

<success_criteria>
- The approved UI design contract is satisfied for copy, colour roles, spacing, typography weights, disabled semantics and hit areas.
- All four ROADMAP Phase 2 success criteria are demonstrated by automated end-to-end tests.
</success_criteria>

<output>
Create `.planning/phases/02-scenario-construction-street-navigation/02-05-SUMMARY.md` when done.
</output>
